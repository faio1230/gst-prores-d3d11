# このリポジトリの作業範囲

- 最終成果物は、GStreamerで単独利用できる純粋Direct3D 11のProRes GPUデコーダー。復号本体はDX11 Compute Shader、通常出力は `GstD3D11Memory` とし、画像のCPU読み戻しを含めない。
- 外部アプリケーション固有の同期、再生制御、UI、入出力、組み込み手順を、このリポジトリの要件・採用基準・引き継ぎ説明へ持ち込まない。検証には独立したGStreamerパイプラインを使う。
- Vulkan版とCPU版は比較参照であり、DX11最終経路の代替や暗黙fallbackにしない。
- 対応範囲、色精度、実表示の欠落、EOS・seek、資源寿命、GPU互換性などの未達事項を隠さず記録し、検証不足の中間成果を完成扱いにしない。
- FFmpegはPATH上の旧版ではなく、リポジトリ内の固定SDKを明示指定する。文書とコミットメッセージは日本語にする。
