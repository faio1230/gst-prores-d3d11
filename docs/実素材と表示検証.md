# 実素材・色・実表示の検証（2026-09-23）

限定対象はprogressive ProRes 422 HQ、10bit、alphaなし。比較と表示はRTX 3070／GStreamer 1.28.2。CPUの画素基準にはリポジトリ内固定SDK `tools/ffmpeg-n8.1-latest-win64-lgpl-shared-8.1/bin/ffmpeg.exe` を明示使用し、PATH上の旧版は使わない。公開素材のバイナリはコミットしない。

## 素材と画素比較

- [Proton Camera公式のサンプル](https://doc.proton-camera.com/docs/samples/)：Atomos Shogun 7収録、1秒に編集したProRes 422 HQ。SDR/REC709版 `media/reference-proton-rec709-hq.mov`、SHA256 `e873a09632c5eb9c3d2d9902af1dd558e70c14d3a458437160a56d91975f03c4`。1920×1080、50fps、50フレーム、BT.709、yuv422p10le。配布元が明記するカメラ由来素材であり、合成テストパターンではない。
- [DJI Inspire 3公式サンプル](https://www.dji.com/inspire-3/samples)：REC.709版の自然映像 `media/reference-dji-nature-4k24-hq.mov`、SHA256 `e97b74358172a82e6b26173cb2f1feba15c321528d49a534a91fccbeb26a8da8`。3840×2160、24fps、129フレーム、BT.709、yuv422p10le。雪山・海・船を実写したカメラ由来素材であることを先頭画像で確認した。ファイルは配布せず、公式リンクから取得する。
- 同じ[DJI Inspire 3公式サンプル](https://www.dji.com/inspire-3/samples/)の[REC.709 4K60原本](https://terra-1-g.djicdn.com/851d20f7b9f64838a34cd02351370894/630sample/Nature072-A006C0035_230328_4U1Q.mov) `Nature072-A006C0035_230328_4U1Q.mov` を `media/reference-dji-nature-4k60-rec709-hq.mov` として取得。SHA256 `247e5527cb5880cd236d35f78418105f936f01a07d1f64818e42823acca84c29`。固定SDKのffprobeで3840×2160、60fps、480フレーム、ProRes 422 HQ、yuv422p10le、BT.709 limitedを確認した。配布元はカメラ撮影の自然映像として掲載している。バイナリはコミットしない。
- [slomo.tv公式サンプル](https://slomotv.ru/support/sample-files)：`media/reference-slomo-4k-hq60-complete.mov`、SHA256 `a38508d1ee876c8d19e3d0d141da00d148f0bc3a1f3de8926089d63b30166f98`。3840×2160、59.94fps、120フレームのHQだが映像は渦巻き検査パターン。4K/60の互換・負荷試験には使い、実写の根拠にはしない。

Atomos MOVはouter frameが512 byte境界まで埋められ、ProRes pictureの後ろに最大509 byteのtrailerを持つ。slomo.tv MOVは16-byte整列で、120フレームのtrailerは50～65 byteの全ゼロだった。parserは512-byte整列で512 byte未満、または512 byte未満の全ゼロtrailerだけを許容し、slice表がpicture末尾へ厳密に一致する条件を維持した。従来の10種に未整列非ゼロ／過大trailerを加えた12種の不正入力を拒否する。

全50フレームのDX11 `I422_10LE(memory:D3D11Memory)` を検査目的でdownloadし、固定FFmpeg CPUの`yuv422p10le`と同じplane／10bit sampleで比較した結果は次のとおり。全フレーム・全sampleで最大差1以内。詳細は `results/proton-real-yuv.json`。

先頭フレームのGPU VLD係数も独立CPU参照と完全一致した（1080p 4,177,920個、4K実写 16,588,800個）。両者ともshader error 0、12種のparser異常を拒否した。詳細は `results/proton-real-coeff.json` と `results/dji-real-4k-coeff.json`。

| plane | 最大絶対差 | MAE | PSNR dB |
|---|---:|---:|---:|
| Y | 1 | 0.03883 | 74.31 |
| U | 1 | 0.03344 | 74.95 |
| V | 1 | 0.03302 | 75.01 |

DJIの4K実写129フレームも同条件で全画素比較し、Y/U/Vすべて最大差1。MAEは0.03752/0.02891/0.03345、PSNRは74.45/75.59/74.95 dB。詳細は `results/dji-real-4k-yuv.json`。これは1080pのAtomosとは別のカメラ・記録経路である。

追加の4K60実写480フレームも固定SDK CPU対DX11で全画素比較し、Y/U/Vすべて最大差1。MAEは0.03529/0.02739/0.02827、PSNRは74.72/75.82/75.68 dB。詳細は `results/dji-real-4k60-yuv.json`。一時RAW 2本で約32GB必要なため、比較器に `--scratch-dir` と空き容量検査を追加し、専用の大容量一時領域を使用した。終了後に中間RAWは自動削除される。

slomo.tv MOVはcontainerの3840×2160に対し、clean-aperture side dataが1888×1062へのcropを指定する。固定FFmpegは通常これを自動適用するが、GStreamerの復号出力はcoded full rasterなので、比較器では `-apply_cropping 0` を明示して解像度を揃える。cropを無視して比較することはしない。
検査パターン120フレームのfull raster Y/U/Vも最大差1で完走した（`results/slomo-4k-pattern-yuv.json`）。これは高周波・16-byteゼロtrailerの互換性検査であり、実写検証数には加えない。

当初は3フレーム目以降、先頭2フレームが交互に再読出しされた。原因はUAV書込み後に再利用`GstD3D11Memory`のstaging copyを無効化していなかったこと。`GST_D3D11_MEMORY_TRANSFER_NEED_DOWNLOAD`を3面へ設定後、全50フレームで最大差1へ復帰した。単一フレーム検査では検出できないため、`scripts/compare-d3d11-real.py`で全フレーム比較する。

RGB10A2は同一DX11復号出力からGPU `d3d11convert` とCPU `videoconvert` に分岐させる。両者の入力はBT.709 limited、出力はfull-range RGB10A2。CPU側は`colorimetry=bt709,chroma-site=jpeg`（水平方向中央）と`dither=none`を明示した。CPU既定の左寄せクロマとGPUの中央補間を混ぜると先頭フレームでR/G/B最大39/12/46まで広がる。位置を揃えた50フレーム比較は以下。詳細は `results/proton-real-rgb.json`。

比較器のRGB出力capsも `colorimetry=1:1:5:1`（full-range RGB、BT.709伝達特性・原色）へ固定し、CPU `videoconvert` は `matrix-mode=full,gamma-mode=none,primaries-mode=none` を明示した。1080p全50枚とDJI 4K全129枚を再測定し、各チャンネルの最大差・差分sample数・MAE・符号付き平均・PSNRは固定前と**完全に同じ**だった。記録は `results/proton-real-rgb-explicit.json` と `results/dji-real-4k-rgb-explicit.json`。従って未固定の出力capsだけが系統差の原因ではない。

このMOVのffprobe `chroma_location` は `unspecified`。中央配置はGPU変換との比較条件であって、カメラ収録の物理的クロマ位置が正しいと確定する情報ではない。従ってこの表は変換器間の数値差であり、表示色の規格適合判定ではない。

| channel | 最大絶対差 | MAE | 符号付き平均（CPU−GPU） | PSNR dB |
|---|---:|---:|---:|---:|
| R | 5 | 3.08898 | +3.08898 | 50.10 |
| G | 2 | 0.36595 | −0.2042 | 63.74 |
| B | 5 | 3.06833 | +3.06833 | 50.11 |
| A | 0 | 0 | 0 | 完全一致 |

`scripts/check-bt709.py`で先頭フレームを明示BT.709 limited→full式とも照合した。CPU/GPUの残差は主にR/B約3 code valueの系統差で、同一変換器での完全一致を意味しない。RGBの採用許容値はまだ合意しておらず、「色精度合格」とはしていない。

DJIの4K実写129フレームでは、同じ明示条件のCPU比でR/G/B/A最大差6/2/5/0、MAE 2.2016/0.8488/2.5558/0、符号付き平均（CPU−GPU）+2.2016/−0.8464/+2.5558/0。詳細は `results/dji-real-4k-rgb.json`。こちらも`chroma_location=unspecified`なので、実際のクロマ位置の規格適合までは断定しない。

### 中性グレーでのrange・行列の切り分け

固定SDKの `prores_ks` でblack / gray / whiteの横3帯（384×128、HQ 1枚）を作り、ProResヘッダーとMOVへBT.709メタデータを入れた。ソースは生成物 `build/rgb-neutral-hq.mov` のみで、実素材の件数には含めない。DX11のI422と固定FFmpeg CPUのY/U/Vは全sampleで**完全一致**（`results/rgb-neutral-yuv.json`）。各帯の内側を測ると以下の値となった（`results/rgb-neutral-diagnostic.json`）。中央帯の期待値は `(504−64)×1023/876` を最近接整数にした514で、Cb/Cr=512のためR/G/B共通となる。

| Y/U/V | 独立BT.709期待RGB | CPU `videoconvert` RGB | GPU `d3d11convert` RGB |
|---|---|---|---|
| 64/512/512 | 0/0/0 | 1/0/1 | 0/0/0 |
| 504/512/512 | 514/514/514 | 515/513/515 | 512/514/512 |
| 940/512/512 | 1023/1023/1023 | 1023/1023/1023 | 1021/1022/1021 |

[GStreamer 1.28.2の10bit平面サンプラ](https://github.com/GStreamer/gstreamer/blob/1.28.2/subprojects/gst-plugins-bad/gst-libs/gst/d3dshader/converter-hlsl/PSMain_converter.hlsl) はR16_UNORMの10bit値を64倍して正規化する。一方、[同版のYUV→RGB行列](https://github.com/GStreamer/gstreamer/blob/1.28.2/subprojects/gst-plugins-bad/gst-libs/gst/d3d11/gstd3d11format.cpp) は10bitの最大値1023を基準に係数を組む。したがって低10bitをR16_UNORMに置いた入力では、GPUが行列に渡す値 `code×64/65535` と理想の `code/1023` が一致しない。この量子化スケール差は白が1023に達しない観測と整合する。CPU経路にも灰色の±1丸め差があるため、実写の全誤差をこの1要因だけに帰属させない。

中性帯はクロマ補間の影響を除けるため、独立した式への許容差を「10bit最終丸めの1 code value以内」と置ける。GPUの中間・白R/Bは2 code value不足し、この厳密基準を満たさない。実写ではクロマ位置が未指定で補間差も含まれるため、max 5～6を安易に採用許容値へ格上げしない。現行 `d3d11convert` によるRGBは、画質測定値は揃ったが色精度の採用判定は保留する。

再現手順（固定SDKを明示指定）：

```powershell
& 'tools/ffmpeg-n8.1-latest-win64-lgpl-shared-8.1/bin/ffmpeg.exe' -hide_banner -loglevel error -f lavfi -i 'color=c=black:s=128x128:r=1' -f lavfi -i 'color=c=gray:s=128x128:r=1' -f lavfi -i 'color=c=white:s=128x128:r=1' -filter_complex '[0:v][1:v][2:v]hstack=inputs=3,format=yuv422p10le,setparams=range=tv:color_primaries=bt709:color_trc=bt709:colorspace=bt709[v]' -map '[v]' -frames:v 1 -c:v prores_ks -profile:v 3 -pix_fmt yuv422p10le -movflags +write_colr -y build/rgb-neutral-hq.mov
python scripts/compare-d3d11-real.py build/rgb-neutral-hq.mov --mode yuv --out results/rgb-neutral-yuv.json
python scripts/compare-d3d11-real.py build/rgb-neutral-hq.mov --mode rgb --frames 1 --diagnostic-dir build/rgb-diagnostic-neutral --out build/rgb-diagnostic-neutral.json
python scripts/summarize-rgb-neutral.py build/rgb-diagnostic-neutral --out results/rgb-neutral-diagnostic.json
python scripts/compare-d3d11-real.py media/reference-proton-rec709-hq.mov --mode rgb --out results/proton-real-rgb-explicit.json
python scripts/compare-d3d11-real.py media/reference-dji-nature-4k24-hq.mov --mode rgb --out results/dji-real-4k-rgb-explicit.json
```

### DX11専用RGB変換の切り分け（検証用）

標準 `d3d11convert` の正規化差を切り分けるため、`src/prores_rgb.hlsl` のSM5 Compute Shaderを追加した。GStreamerのDX11デコーダーが出す3面R16_UNORM I422から10bit整数コードを復元し、`chroma-site=jpeg` 相当の水平中央補間、BT.709 limited YUV→full RGBを計算し、同一D3D11 device上のRGB10A2 typed UAVテクスチャへ書く。RTX 3070ではこのUAV形式を作成できた。`tests/d3d11_rgb_probe.cpp` は各フレームのD3D11Memory・色メタデータ・寸法・deviceを検査し、最後にstagingへ読み戻す。読み戻しは検証器だけで行い、通常の復号経路には加えない。CPU/Vulkanへの復号fallbackはない。

中性3帯のRGBは黒 `0/0/0`、灰 `514/514/514`、白 `1023/1023/1023` で、独立BT.709式と一致した（`results/rgb-neutral-native-dx11.json`）。`scripts/check-bt709.py` の左端クロマ補間は、clamp後のindexに1を足していた誤りを修正し、境界外の隣接2点をそれぞれclampするようにした。実写ではDX11復号I422を別途downloadした整数コードにPythonのfloat32 BT.709式を適用し、GPU出力の全画素と比較した。符号付き差は基準−GPU。許容差1 codeは10bit最終丸めとGPU/CPU浮動小数演算の差に限定し、実際の各色チャンネル最大差はすべて1だった。

| 素材と比較基準 | 枚数 | R/G/B/A最大差 | R/G/B MAE |
|---|---:|---:|---:|
| Proton 1080p、独立BT.709式−DX11専用RGB | 50 | 1/1/1/0 | 0.0310/0.0304/0.0320 |
| DJI 4K、独立BT.709式−DX11専用RGB | 129 | 1/1/1/0 | 0.0311/0.0311/0.0313 |
| DJI 4K60、独立BT.709式−GStreamer DX11専用RGB要素 | 480 | 1/1/1/0 | 0.0310/0.0312/0.0313 |
| Proton 1080p、GStreamer CPU `videoconvert`−DX11専用RGB | 50 | 3/2/2/0 | 1.7154/0.6147/1.5739 |
| DJI 4K、GStreamer CPU `videoconvert`−DX11専用RGB | 129 | 4/2/3/0 | 1.2007/0.9600/1.3209 |

全フレームの差分sample数、符号付き平均、PSNR、実行コマンドは `results/proton-real-rgb-native-formula.json`、`results/dji-real-4k-rgb-native-formula.json`、`results/proton-real-rgb-native.json`、`results/dji-real-4k-rgb-native.json` に保存した。数式比較は同じDX11復号I422から始めるためRGB変換だけを隔離する。固定SDK CPU復号とのYUV比較は別の全フレーム検査で各面最大1を確認済み。CPU `videoconvert` 自身にも丸めと行列実装の差があり、カメラ素材の `chroma_location` は未指定である。したがって「独立式と1以内」は明示した中央クロマ・BT.709条件での数値的検証であり、撮影時の色意図や表示機器の色管理まで証明しない。この段階では専用RGBテクスチャをGStreamerへ出していなかったが、下記の要素統合で解消した。標準 `d3d11convert` の表示経路には旧来の色差が残る。

追加の4K60は統合済みの `proresd3d11rgb` 要素そのものの `RGB10A2_LE(memory:D3D11Memory)` を読み戻し、全480枚を独立式と比較した。詳細は `results/dji-real-4k60-rgb-formula.json`。これは上記の初期shader単体検証と異なり、GStreamer要素としての連続出力を含む。ただしこの検査だけでOS実表示の欠落がないとは言えない。

再現手順（`./scripts/build.ps1` の後。比較器がGStreamer環境と固定SDKを指定する）：

```powershell
python scripts/compare-d3d11-real.py build/rgb-neutral-hq.mov --mode rgb_native --frames 1 --diagnostic-dir build/rgb-diagnostic-neutral-native --out results/rgb-neutral-native-comparison.json
python scripts/summarize-rgb-neutral.py build/rgb-diagnostic-neutral-native --out results/rgb-neutral-native-dx11.json
python scripts/compare-d3d11-real.py media/reference-proton-rec709-hq.mov --mode rgb_formula --out results/proton-real-rgb-native-formula.json
python scripts/compare-d3d11-real.py media/reference-dji-nature-4k24-hq.mov --mode rgb_formula --out results/dji-real-4k-rgb-native-formula.json
python scripts/compare-d3d11-real.py media/reference-proton-rec709-hq.mov --mode rgb_native --out results/proton-real-rgb-native.json
python scripts/compare-d3d11-real.py media/reference-dji-nature-4k24-hq.mov --mode rgb_native --out results/dji-real-4k-rgb-native.json
```

### `proresd3d11rgb` のD3D11Memory出力

同じshaderをGStreamer `GstBaseTransform` 要素 `proresd3d11rgb` に組み込んだ。`proresd3d11dec ! proresd3d11rgb` で、入力の3面R16_UNORMと同じD3D11 device上のpoolが1面RGB10A2 typed UAVを確保し、GPUから直接書く。入力・出力のD3D11Memory caps、range・matrix・transfer・primaries、形式、寸法、deviceを検査する。GPU書込み後はpoolメモリのstaging読み戻しキャッシュを無効化する。初回の実表示計測で要素がPTSを落としていたため、入力の時刻・buffer flagを明示コピーする修正を加えた。標準 `d3d11convert` に暗黙fallbackせず、未対応形式・色条件・typed UAV非対応GPUではエラーにする。

公開Proton全50枚とDJI全129枚のD3D11Memory出力を検査時に読み戻して、独立BT.709式とのR/G/B/A最大差1/1/1/0、MAEは1080p 0.0310/0.0304/0.0320、4K 0.0311/0.0311/0.0313だった。GStreamer CPU `videoconvert` 比はそれぞれ最大3/2/2/0と4/2/3/0、MAE 1.7154/0.6147/1.5739と1.2007/0.9600/1.3209。検証用の直接shaderテクスチャと中性帯1枚のRGB出力はSHA256まで一致。全フレームの差分sample数・PSNR・実行コマンドは `results/proton-real-rgb-element-formula.json`、`results/dji-real-4k-rgb-element-formula.json`、`results/proton-real-rgb-element-cpu.json`、`results/dji-real-4k-rgb-element-cpu.json`。通常経路は画像をCPUへ読まない。

差分sample数（R/G/B、Aは0）は、独立式比でProton `3,212,186/3,154,934/3,316,897`（各チャンネル母数103,680,000）、DJI `33,238,630/33,236,105/33,536,937`（各チャンネル母数1,069,977,600）。CPU `videoconvert` 比ではProton `103,571,567/60,976,172/103,091,813`、DJI `1,065,910,181/977,105,367/1,067,931,152`。独立式の最大1 codeを変換の数値的許容差とし、CPU実装との最大3～4 code差は参照実装の丸め・行列差として別管理する。カメラ側の実クロマ位置が未指定のため、これだけで撮影色の規格適合を宣言しない。

`tests/d3d11_rgb_element_smoke.cpp` では1080p50枚・4K129枚のEOS、単一device上のRGB10A2 D3D11Memory、PTS単調増加、先頭へのflushing seek、pipeline停止・破棄後の保持buffer読み戻しを確認した。合成180枚の1080p/4Kでも同条件で回帰する。元の合成MOVはtransfer/primariesが未指定なので、固定SDKのstream copyでMOVへBT.709 metadataを付けた検査用ファイルを`build/vs18`に作る。圧縮画像そのものは再符号化しない。`scripts/test-d3d11-plugin.ps1` がこの操作とRGB回帰を自動実行する。

```powershell
./scripts/build.ps1
./scripts/test-d3d11-plugin.ps1 -OutDir results/verification-rgb-element-2026-09-23
python scripts/compare-d3d11-real.py build/rgb-neutral-hq.mov --mode rgb_element --frames 1 --diagnostic-dir build/rgb-diagnostic-neutral-element --out results/rgb-neutral-element-comparison.json
(Get-FileHash build/rgb-diagnostic-neutral-element/gpu-rgb10a2.raw -Algorithm SHA256).Hash
(Get-FileHash build/rgb-diagnostic-neutral-native/gpu-rgb10a2.raw -Algorithm SHA256).Hash
python scripts/compare-d3d11-real.py media/reference-proton-rec709-hq.mov --mode rgb_element_formula --out results/proton-real-rgb-element-formula.json
python scripts/compare-d3d11-real.py media/reference-dji-nature-4k24-hq.mov --mode rgb_element_formula --out results/dji-real-4k-rgb-element-formula.json
python scripts/compare-d3d11-real.py media/reference-proton-rec709-hq.mov --mode rgb_element --out results/proton-real-rgb-element-cpu.json
python scripts/compare-d3d11-real.py media/reference-dji-nature-4k24-hq.mov --mode rgb_element --out results/dji-real-4k-rgb-element-cpu.json
```

## 実表示とデコーダー単体の分離

`scripts/benchmark-d3d11-display.py`はDX11復号→D3D11 RGB10A2→`d3d11videosink sync=true`を実ウィンドウ／swapchainへ接続し、`present`信号、sinkのrendered/dropped、QoS、CPU、メモリ、nvidia-smiを保存する。WMI報告の現在のディスプレイ設定は3840×2160／60Hz。`present`信号はswapchainへのpresent要求前の時刻であり、物理パネルの発光時刻ではない。各周の最初30件をinterval統計から除外する。seek時にsink統計がリセットされるので周ごとに記録・合算する。

合成60fps素材を3試行×3周×180フレーム表示した結果。p95/p99、seek、CPU/GPU/メモリは3試行の中央値。GPU使用率・VRAMは他プロセスを含むGPU全体値。原データは `results/d3d11-display/`。

| 素材 | 表示/予定 | drop（試行別） | present p95/p99 ms | seek先頭 p95 ms | CPU全体 % | GPU平均 % | peak WS MiB |
|---|---:|---|---:|---:|---:|---:|---:|
| 1080p60 | 540/540 ×3 | 0, 0, 0 | 17.72 / 18.17 | 4.74 | 1.26 | 17.68 | 60.90 |
| 4K60 | 540/540、525/540、540/540 | 0, 14, 0 | 17.80 / 18.37 | 10.48 | 1.98 | 27.30 | 102.82 |

4Kの1試行2周目にはpresent間隔262.7msの単発停止、QoS 15件、drop 14件が生じた。表示p99は残ったpresentだけの統計なので、この停止を表現しない。最大intervalとdropを併記し、安定60fpsの条件は未達として追加反復する。1080pの実写50fpsは3周・150/150表示、drop 0、present p99 21.46ms。

この後の4K追加6試行×540フレーム（`results/d3d11-display-extra/`）は全てdrop 0で、単発停止は再現しなかった。合計9試行中1試行の異常として残し、原因未特定のまま「必ずdrop 0」とは記載しない。

計測器に圧縮入力・DX11デコーダー出力・D3D11 RGB変換出力のPTSと時刻を追加し、合成4K60でさらに12試行×3周×180枚を実行した。結果は予定6480枚、表示6478枚、sink申告drop 0、QoS 2件。12試行中2試行で各1枚が圧縮入力にはあり、デコーダー出力とRGB出力にはなかった（PTSは3周目1.550秒、2周目1.683秒）。欠落直前の圧縮入力→デコーダー出力に22.44/23.28msを要した。`sink dropped=0`はend-to-end無欠落を保証しない。今回の中央値present p95/p99は17.80/18.32ms、最大間隔25.58ms。元の263ms停止は再現せず、根本原因は未特定。時系列と試行別集計は `results/d3d11-display-stages-repeat/` と `results/d3d11-display-stages-repeat-summary.json`。段階別ログはpad probeによるCPU到達時刻で、GPU完了時刻ではない。

同じ計測器で実素材と1080p60も各3試行測り直した。以下のCPUはマシン全体換算のプロセスCPU、GPUは他プロセス込みのデバイス全体サンプリング、WSは当該プロセスpeak working set。p95/p99、seek、CPU/GPU/WSは試行中央値だが、最大present間隔は全試行の最大値。PTS段階ログから算出した全経路の表示枚数を基準とする。

| 素材・条件 | 表示/予定 | sink drop | present p95/p99 ms | 最大間隔 ms | seek先頭p95 ms | CPU/GPU % | peak WS MiB |
|---|---:|---:|---:|---:|---:|---:|---:|
| 合成1080p60、3試行 | 1620/1620 | 0 | 17.79/18.31 | 18.99 | 3.69 | 1.21/17.19 | 62.50 |
| 合成4K60、12試行 | 6478/6480 | 0 | 17.80/18.32 | 25.58 | 8.40 | 2.05/27.56 | 102.90 |
| Proton実写1080p50、3試行 | 450/450 | 0 | 21.26/21.47 | 21.73 | 4.41 | 1.00/11.24 | 61.26 |
| DJI実写4K24、3試行 | 1159/1161 | 1 | 42.72/43.20 | 111.43 | 13.03 | 1.24/13.33 | 114.61 |
| slomo検査パターン4K59.94、3試行 | 1080/1080 | 0 | 17.77/18.18 | 18.69 | 8.60 | 2.18/27.13 | 109.92 |

DJIの欠落試行では初周のPTS 0.125秒が圧縮入力後に消え、さらにsinkが1枚をdropした。正常試行の最初のpresentは約126ms、欠落試行は約362msで、その直後のpresent間隔は111ms。段階ログでは初周0→0.0417秒の圧縮入力が263ms空き、デコーダー出力→RGB出力も一度86.7msかかった。別のDJI追加6試行でも1試行で同じ初周PTS 0.125秒が欠け、最初のpresentが約362ms、最大間隔110msだった（`results/d3d11-display-dji-repeat-summary.json`）。初期表示・GPU処理の遅延と整合するが、ドライバー／OS／要素の責任分界は未確定。

PAUSED preroll後にPLAYINGへ遷移する9試行でも1試行で2枚欠け、最大present間隔112msだった（`results/d3d11-display-dji-preroll-repeat-summary.json`）。prerollだけでは対策にならない。診断用にsinkのQoSを無効化し `max-lateness=-1` としたDJIの9試行では3483/3483枚を表示したものの、最大間隔101msが残った（`results/d3d11-display-dji-lossless-repeat-summary.json`）。これは欠落防止と引き換えに停止が見える条件で、滑らかな表示の合格ではない。同設定の合成4K60は6試行3240/3240枚、最大間隔19.61msだったが、通常設定での稀な停止を否定する試行数ではない。

```powershell
python scripts/benchmark-d3d11-display.py media/synthetic-2160p60-hq.mov --loops 3 --repeats 12 --out results/d3d11-display-stages-repeat
python scripts/summarize-d3d11-display.py results/d3d11-display-stages-repeat --out results/d3d11-display-stages-repeat-summary.json
python scripts/benchmark-d3d11-display.py media/reference-dji-nature-4k24-hq.mov --loops 3 --repeats 9 --preroll --out results/d3d11-display-dji-preroll-repeat
python scripts/benchmark-d3d11-display.py media/reference-dji-nature-4k24-hq.mov --loops 3 --repeats 9 --lossless-sink-policy --out results/d3d11-display-dji-lossless-repeat
```

公開素材の実表示も実施した。DJI 4K実写24fpsは3周387/387表示、drop 0、present p95/p99 42.82/43.47ms。slomo.tvの4K検査パターン59.94fpsは3周360/360表示、drop 0、p95/p99 17.76/18.28ms。記録は `results/d3d11-display-public/`。これらはそれぞれ実写24fpsとパターン約60fpsであり、「4K実写60fps」を単一素材で検証した結果ではない。

同じ合成素材で表示待ちを除く`dx11-direct`を別に計測すると、1080p 320.3fps／p99 3.55ms、4K 146.7fps／p99 7.42ms。これはGPU完了待ちなしのbuffer供給であり、表示やGPU完了のp99へ流用しない。表示の約16.7ms間隔は主として60fps clock pacingだが、4K単発停止の原因はまだ切り分け中。

### 専用RGB D3D11Memory経路の実表示

`d3d11_display_bench` に `native-rgb` 選択を加え、`proresd3d11dec ! proresd3d11rgb ! d3d11videosink` を実ウィンドウへ表示した。元の合成60fps ProResはtransfer/primaries未指定のため、固定SDKのstream copyでMOVのBT.709タグだけを補った。圧縮画素は同一で、タグ付き1080p/4KのSHA256はそれぞれ `b7d58e27638c67f6b3460449282737509fe2bdfc2f7cd03a5da8db6fa941c84e` / `6ac10b7cf35b0f094f67c357e0a77ebe8ff8bf98828f295fd23bfb51aebd4358`。画素側の検査には公開実写を使い、60fps表示の性能側にはこの合成素材を使い分けた。

各試行は180枚×3周、seekあり、60Hz画面、`sync=true`、通常QoS。p95/p99は各周先頭30 intervalを除いたpresent要求時刻、CPUは全マシン換算の当該プロセス、GPUは他プロセス込みのRTX 3070全体サンプリング。p95/p99・seek・CPU/GPU・WSは試行中央値、最大間隔は全試行最大値。direct fakesinkのwall throughputは復号＋専用RGBで1080p 184.7fps、4K 107.8fpsだったが、GPU完了時間でも表示時の無欠落保証でもない。

| RGB経路と素材 | 表示/予定 | sink drop | present p95/p99 ms | 最大間隔 ms | seek先頭p95 ms | CPU/GPU % | peak WS MiB |
|---|---:|---:|---:|---:|---:|---:|---:|
| 専用DX11、1080p60、3試行 | 1620/1620 | 0 | 17.78/18.24 | 19.08 | 4.36 | 1.31/18.32 | 60.22 |
| 標準 `d3d11convert`、1080p60、3試行 | 1620/1620 | 0 | 17.83/18.30 | 18.80 | 4.27 | 1.19/17.26 | 61.66 |
| 専用DX11、4K60、初回3試行 | 1609/1620 | 0 | 17.75/18.21 | 102.20 | 7.44 | 1.85/28.64 | 103.01 |
| 専用DX11、4K60、追加6試行 | 3240/3240 | 0 | 17.80/18.33 | 23.94 | 7.89 | 2.03/27.93 | 103.10 |
| 標準 `d3d11convert`、4K60、3試行 | 1620/1620 | 0 | 17.83/18.28 | 20.45 | 7.10 | 1.89/27.49 | 102.77 |
| 標準 `d3d11convert`、4K60、追加6試行 | 3240/3240 | 0 | 17.79/18.27 | 23.22 | 12.37 | 1.94/27.51 | 105.91 |

専用DX11の4K初回3試行では2試行に計11枚の欠落。sink申告dropは0だが、段階別PTSでは圧縮入力後・復号出力前に1枚と10枚が欠け、復号出力後・RGB出力前の欠落は0だった。10枚連続欠けた試行は圧縮入力padで106.7ms、decoded padで113.35msの間隔があり、最大present間隔も102.2ms。長い入力pad間隔は下流の処理待ちから逆流し得るため、外部I/O停止ともRGB要素の責任とも断定できない。標準経路は今回の9試行で0欠落だが、過去の別測定では4Kの14 dropや段階別の2枚欠落を確認している。よってこの短い非交互のA/Bから安定性の優劣を結論づけない。残存presentだけのp99を「無停止」と読まず、end-to-end枚数と最大間隔を併記する。

生データと段階別の集計は `results/d3d11-display-native-rgb/`、`results/d3d11-display-native-rgb-repeat/`、`results/d3d11-display-standard-matched/`、`results/d3d11-display-standard-matched-repeat/`、各 `*-summary.json` に保存した。再現時は `./scripts/test-d3d11-plugin.ps1` が作る `build/vs18/synthetic-1080p60-bt709.mov` と `build/vs18/synthetic-2160p60-bt709.mov` を指定する（上記とSHA256一致）。

```powershell
python scripts/benchmark-d3d11-display.py build/vs18/synthetic-1080p60-bt709.mov build/vs18/synthetic-2160p60-bt709.mov --loops 3 --repeats 3 --native-rgb --out results/d3d11-display-native-rgb
python scripts/benchmark-d3d11-display.py build/vs18/synthetic-2160p60-bt709.mov --loops 3 --repeats 6 --native-rgb --out results/d3d11-display-native-rgb-repeat
python scripts/benchmark-d3d11-display.py build/vs18/synthetic-1080p60-bt709.mov build/vs18/synthetic-2160p60-bt709.mov --loops 3 --repeats 3 --out results/d3d11-display-standard-matched
python scripts/benchmark-d3d11-display.py build/vs18/synthetic-2160p60-bt709.mov --loops 3 --repeats 6 --out results/d3d11-display-standard-matched-repeat
python scripts/summarize-d3d11-display.py results/d3d11-display-native-rgb --stem synthetic-2160p60-bt709 --out results/d3d11-display-native-rgb-4k-summary.json
```

### 4K表示停止の前後段とQoSの診断

`d3d11_display_bench` に、qtdemuxとdecoderの間に32 bufferのqueueを置く診断条件を追加した。queue入力を `demuxed`、decoder入力を `compressed` として同じPTSの到達時刻を保存する。専用RGB・通常QoS・4K60の12試行×3周は6468/6480枚で、1試行だけ12枚欠落。sink申告dropは0、12枚はすべてdecoder出力前、decoder出力後・RGB変換前は0。中央値present p95/p99は17.75/18.28ms、最大present間隔119.23ms、seek先頭p95 7.88ms、CPU/GPU平均2.04/27.99%、peak WS中央値143.85MiB（queueなし約103MiB）。queue追加だけでは停止を除去できなかった。記録は `results/d3d11-display-native-rgb-queue12/` と `results/d3d11-display-native-rgb-queue12-summary.json`。

欠落試行 `-4` では、loop 1のPTS 1.050→1.0667秒のdecoder入力間隔が115.26ms、PTS 1.600→1.6167秒のqueue入力間隔が115.40ms。queue入力は約0.55秒先を保持していたため、異なるPTSで同時刻帯に停止した。該当フレームのRGB出力は計測原点から約4164.67ms、`present`信号は4174.49ms、次のdecoder入力は4273.99ms。つまり該当フレームの復号とRGB計算は終わり、その下流から戻るまでに長い間隔が生じた。queueが満杯になればdemux側にも待ちが逆流するため、decoder入力とdemux入力の同時停止を「上流I/O障害」とは断定しない。[GStreamerのpresent信号](https://gstreamer.freedesktop.org/documentation/d3d11/d3d11videosink.html) は`IDXGISwapChain::Present`直前の通知で、物理表示時刻でもPresent復帰時刻でもない。現ログだけではsink内部・ドライバー・OSのどこで止まったかは特定できない。

[GstVideoDecoderのQoS設定](https://gstreamer.freedesktop.org/documentation/video/gstvideodecoder.html) は下流QoSから遅延フレームの出力破棄を行う。これをdecoderだけ無効化し、sink側の通常QoSを保ったqueue付き4K60・12試行では6480/6480枚、最大present間隔22.49msだった。ただしこの12試行では自然の長い停止が再現せず、QoS無効で安定表示できた証明ではない。因果経路を確認するため、検査器のsink入力でPTS 1秒に一度だけ120ms待つ診断を行った。同じ4K/3周で通常decoder QoSは527/540枚、decoder段階13枚破棄、QoS 13件、最大present間隔126.40ms。decoder QoS無効では540/540枚だが最大present間隔127.46msで、停止自体は残った。人工停止を自然停止の原因とみなさず、「長い待ち→QoSでdecoderが破棄」という動作だけを検証した。記録は `results/d3d11-display-native-rgb-decoder-no-qos12/` と `results/d3d11-display-injected-*/` および各summary JSON。

専用RGB経路で公開実写も表示した。Proton 1080p50は通常QoS・3試行で434/450枚、1試行の起動直後に約206msのdecoder入力間隔と16枚欠落、最大present間隔184.36ms。DJI 4K24は3試行1161/1161枚だが、1試行の最初のdecoder入力間隔は354msあり、最初のpresent以前の初期化遅延を含む。Protonの明示PAUSED preroll 9試行は1344/1350枚で1試行6枚欠落、最大present間隔82.71ms。prerollにdecoder QoS無効を加えた別の9試行は1350/1350枚、最大26.62msだったが、こちらも長い自然停止が再現しなかった。合成と実写のいずれでも、短いpresent p99だけでは起動遅延や稀な欠落を否定できない。試行別のPTS、present、GPU記録は `results/d3d11-display-native-rgb-real/`、`results/d3d11-display-native-rgb-proton-preroll9/`、`results/d3d11-display-native-rgb-proton-preroll-no-qos9/` と各summary JSON。

```powershell
python scripts/benchmark-d3d11-display.py build/vs18/synthetic-2160p60-bt709.mov --loops 3 --repeats 12 --native-rgb --queue-before-decoder --out results/d3d11-display-native-rgb-queue12
python scripts/benchmark-d3d11-display.py build/vs18/synthetic-2160p60-bt709.mov --loops 3 --repeats 12 --native-rgb --queue-before-decoder --decoder-no-qos --out results/d3d11-display-native-rgb-decoder-no-qos12
python scripts/benchmark-d3d11-display.py build/vs18/synthetic-2160p60-bt709.mov --loops 3 --repeats 1 --native-rgb --queue-before-decoder --sink-stall-ms 120 --out results/d3d11-display-injected-qos-on
python scripts/benchmark-d3d11-display.py build/vs18/synthetic-2160p60-bt709.mov --loops 3 --repeats 1 --native-rgb --queue-before-decoder --decoder-no-qos --sink-stall-ms 120 --out results/d3d11-display-injected-decoder-no-qos
python scripts/benchmark-d3d11-display.py media/reference-proton-rec709-hq.mov --loops 3 --repeats 9 --native-rgb --preroll --out results/d3d11-display-native-rgb-proton-preroll9
```

### sinkへのpush復帰時刻

診断時だけRGB要素と `d3d11videosink` の間に、buffer・event・queryをそのまま通す `d3d11pushmeter` を置いた。RGB出力後の `sink_push` と `gst_pad_push` 復帰後の `sink_return` を同一PTSで記録する。これは**sinkへの同期呼び出し全体**の時間であり、`IDXGISwapChain::Present` 単体の所要時間でも物理表示時刻でもない。120msのsink入力人工停止ではpush最大120.80ms、decoder段階13枚欠落となり、前節の人工停止と一致。新しい37試行すべてで `sink_push`／`sink_return` の件数は表示枚数と一致した。

人工停止なしのqueue付き合成4K60・12試行×3周は6480/6480枚、sink drop 0。sink pushのp99中央値12.24ms、最大19.18ms、present間隔p95/p99中央値17.74/18.20ms、最大22.48ms、CPU/GPU中央値2.03/28.39%、peak WS中央値159.86MiB、seek先頭p95中央値7.92ms。この試行では長い自然停止が起きなかった。前節のqueue付き通常QoS 12試行では12枚欠落しており、計測要素が停止を解決したとは判断しない。記録は `results/d3d11-display-return-4k12/` と同名summary JSON。

公開Proton実写1080p50・通常再生12試行×3周は1783/1800枚、1試行で17枚欠落、QoS 17件、sink drop 0。欠落試行の先頭PTS 0はRGB出力109.684ms、sink push開始109.687ms、`present`通知124.790ms、push復帰313.240ms。push内部で203.55ms、そのうち通知後から復帰まで約188.45msを占めた。次の圧縮PTS 20msのdecoder入力は313.559msで、復帰直後に再開。PTS 40～360msの17枚はdecoder入力後・出力前に欠けた。さらにPTS 380msのpushにも約76msかかった。復号・RGB計算後の下流待ちが上流を止めることを、自然停止で直接確認した。ただし通知後の待ちを`Present`呼び出し本体・sink後処理・ドライバーのいずれかへ単独帰属させる証拠はない。全12試行のpresent p95/p99中央値は21.16/21.66msだが最大間隔190.89ms、push最大203.55ms。記録は `results/d3d11-display-return-proton12/` と同名summary JSON。

Protonの明示PAUSED preroll＋同計測12試行は1799/1800枚、1試行でQoS 1件。該当試行では先頭PTS 0のpushがpreroll中に213.94msを要した。ただし欠けたPTS 60msは後の復号区間約29msを伴い、preroll中の長いpushとの因果は断定しない。全試行のpresent最大間隔29.90msだけを見ても、PLAYING前の213.94ms待ちを見落とす。記録は `results/d3d11-display-return-proton-preroll12/` と同名summary JSON。

```powershell
python scripts/benchmark-d3d11-display.py build/vs18/synthetic-2160p60-bt709.mov --loops 3 --repeats 12 --native-rgb --queue-before-decoder --trace-sink-return --out results/d3d11-display-return-4k12
python scripts/benchmark-d3d11-display.py media/reference-proton-rec709-hq.mov --loops 3 --repeats 12 --native-rgb --trace-sink-return --out results/d3d11-display-return-proton12
python scripts/benchmark-d3d11-display.py media/reference-proton-rec709-hq.mov --loops 3 --repeats 12 --native-rgb --preroll --trace-sink-return --out results/d3d11-display-return-proton-preroll12
python scripts/summarize-d3d11-display.py results/d3d11-display-return-proton12 --out results/d3d11-display-return-proton12-summary.json
```

この診断要素を追加した後に `./scripts/build.ps1`、`./scripts/test-dx11.ps1 -OutDir results/verification-sink-return-2026-09-23`、`./scripts/test-d3d11-plugin.ps1 -OutDir results/verification-sink-return-2026-09-23` を実行して通過。合成1080p/4Kの全係数一致・YUV最大差1、D3D11Memory 3面UAV、EOS 180枚×3、4位置seek、buffer寿命、6エラー経路、専用RGBのEOS/seekを維持した。`python -m py_compile` と `git diff --check` も通過。新集計器で前節の旧ログ6480枚／6468枚を再読して同じ欠落12枚を得た。計測要素は検査実行ファイル内だけに登録し、製品pluginと通常パイプラインには加えない。

### OS表示時刻とのPTS照合

[Intel PresentMon 2.6.0](https://github.com/GameTechDev/PresentMon/releases/tag/v2.6.0) の公式console版を検査用に使用した。実行ファイルのSHA256は `b2a706bc6ad475749e3b7e3409263aa1e6906d45bdcf993f6dbc0f660188f1af`。`build/vs18/diagnostic-tools/` に置いたバイナリはコミットしない。3840×2160／60HzのRTX 3070接続画面で、通常QoS、`sync=true`、専用DX11 RGB、3周とseek、sink push計測を同時に行った。素材は上記の公式公開Atomos実写と、固定SDKで作った合成1080p60／4K60を使い分けた。4K60はカメラ実写の証拠ではない。

検査器の`present`通知へPTSとQPC基準時刻を記録し、PresentMonの`CPUStartQPCTimeInMs + MsCPUBusy`をPIDごとに1ms以内で照合した。全3素材のPresentMon行に照合漏れはなく、最大時刻差は0.14ms。各seek周の最初・最後のPTSはEOS／再表示境界を含むので、以下はそれらを除いた内側フレームである。[PresentMonのCSV定義](https://github.com/GameTechDev/PresentMon/blob/main/README-ConsoleApplication.md#csv-columns) に従い、`MsUntilDisplayed=NA`を「OS表示時刻なし」と読む。これはGStreamerのrendered数や肉眼／光学的な表示測定とは別の指標。[GStreamer 1.28.2のwindowコード](https://github.com/GStreamer/gstreamer/blob/1.28.2/subprojects/gst-plugins-bad/sys/d3d11/gstd3d11window.cpp) では`present`通知は実際のPresent呼び出し前に出るため、通知時刻を表示時刻とみなさない。

| 素材・試行 | GStreamer rendered/予定 | 内側PTSのOS表示時刻あり/対象 | OS表示時刻なし | OS表示間隔p95/p99中央値 ms | OS最大間隔 ms | Present API最大 ms |
|---|---:|---:|---:|---:|---:|---:|
| 合成1080p60・3試行 | 1620/1620 | 1549/1602 | 53 | 16.77/33.31 | 33.44 | 1.31 |
| 合成4K60・3試行 | 1620/1620 | 1587/1602 | 15 | 16.70/16.74 | 33.34 | 0.82 |
| 公開Atomos 1080p50・12試行 | 1797/1800 | 1725/1725 | 0 | 33.34/33.39 | 50.02 | 1.36 |

3素材とも内側PTSのOS捕捉漏れは0。1080p60の53枚は3試行中43枚が1試行へ集中し、4K60の15枚も1試行へ集中した。どちらもGStreamerのsink dropとdecoder QoSは0で、Present API呼び出しの最大値も1.4ms未満。従って「GStreamer rendered/drop 0」はOS上の全フレーム表示を保証しない。一方、実写の予定1800枚との差3枚は起動時のdecoder QoSで復号出力前に欠けたPTS 60/100/120msであり、PresentMonに捕捉された内側のフレームは全てOS表示時刻を得た。50fps素材を60Hz画面へ出すため、同素材の33ms付近の表示間隔をそのまま復号停止とは解釈しない。3素材のPresentModeは`Composed: Flip`で、ウィンドウの前面状態・遮蔽は固定していない。OS合成やウィンドウ状態が未表示の候補だが、原因は確定していない。

同じ試行のGStreamer`present`通知間隔p95/p99中央値は合成1080p60で17.74/18.19ms、合成4K60で17.74/18.21ms、実写1080p50で21.08/21.39ms。seek後先頭通知p95中央値は4.84/7.24/4.16ms、CPU全マシン換算1.36/2.07/1.21%、GPU全体使用率平均の試行中央値17.45/28.45/11.89%、peak working set中央値59.99/140.85/61.34MiB。順序はこの段落の3素材順。GPU値は他プロセスも含むため、デコーダー固有の使用率ではない。

今回の実写欠落試行では先頭PTS 0のsink pushが248.35ms。push開始105.623msに対し`present`通知は307.093msと353.666msの2回で、最初の通知まで約201ms、最後の通知からpush復帰353.969msまでは約0.30msだった。同一試行のPresent API最大は0.58ms。前節の別試行では通知**後**から復帰まで約188ms待った。自然停止が常にPresent API本体で起きる、または常に通知前に起きる、とは断定できない。まず同時ETWでGStreamer sink内部のwindow show/render、GPU完了、DWMの段階を分ける必要がある。

生CSV・各試行のPTS/stage/GPU記録と集計JSONは `results/presentmon-{1080p60,4k60,proton}-qpc*`。同時捕捉の再現例は以下。PresentMonは別のPowerShellで先に起動し、終了まで動かす。実素材は上記の公式取得元を使い、PATHのFFmpegは使用しない。

```powershell
& 'build/vs18/diagnostic-tools/PresentMon-2.6.0-x64.exe' --process_name d3d11_display_bench.exe --output_file results/presentmon-4k60-qpc.csv --qpc_time_ms --write_display_metadata --timed 52 --terminate_after_timed --no_console_stats --session_name ProResDX114KQPC
python scripts/benchmark-d3d11-display.py build/vs18/synthetic-2160p60-bt709.mov --loops 3 --repeats 3 --native-rgb --trace-sink-return --out results/presentmon-4k60-qpc-bench
python scripts/summarize-presentmon-display.py results/presentmon-4k60-qpc.csv results/presentmon-4k60-qpc-bench --out results/presentmon-4k60-qpc-summary.json
```

PTS計測を追加した後に `./scripts/test-dx11.ps1 -OutDir results/verification-presentmon-2026-09-23` と `./scripts/test-d3d11-plugin.ps1 -OutDir results/verification-presentmon-2026-09-23` を再実行して通過。合成1080p/4Kの係数完全一致・YUV最大差1、D3D11Memory 3面UAV、EOS・4位置seek・buffer寿命・6エラー経路、専用RGBのEOS/seekを維持した。新集計器の3素材再集計と `python -m py_compile`、`git diff --check` も通過。OS表示時刻の欠落と長いsink待ちは、この回帰検査では解消していない。

### ウィンドウ状態を伴うOS表示再測定

検査器に `--trace-window-state` を追加し、各`present`通知時の自プロセス内 `GSTD3D11` ウィンドウについて可視、最小化、前面、サイズを読み取った。ウィンドウの位置・Z順・focusは変更しない。[GStreamer 1.28.2のWin32実装](https://github.com/GStreamer/gstreamer/blob/1.28.2/subprojects/gst-plugins-bad/sys/d3d11/gstd3d11window_win32.cpp) がこのclass名の内部ウィンドウを作る。初回だけ列挙してHWNDを再利用する。毎フレーム列挙した試験では時刻照合に1ms超のずれが生じたため破棄し、以下は全PresentMon行の照合に成功した再試験のみ。

合成1080p60と4K60を各12試行×3周×180枚、通常QoS・`sync=true`・専用RGBで測った。各試行でGStreamerは540/540枚、sink dropとdecoder QoSは0。PIDが別試行で再利用されたため、集計器はPIDに加え、PresentMon QPC時刻をその試行の`present`通知時刻範囲で絞るよう修正した。1080pの6492行／4Kの6492行を各試行へ正しく帰属し、全行がPTSへ1ms以内で照合された（最大差0.124／0.170ms）。内側フレームのOS捕捉漏れは0。

| 合成素材 | GStreamer rendered/予定 | 内側PTSのOS表示時刻あり/対象 | OS表示時刻なし | OS最大表示間隔 ms | Present API最大 ms | sink push最大 ms |
|---|---:|---:|---:|---:|---:|---:|
| 1080p60 | 6480/6480 | 6303/6408 | 105 | 50.01 | 1.37 | 19.01 |
| 4K60 | 6480/6480 | 6349/6408 | 59 | 38.32 | 1.01 | 22.59 |

1080pの未表示105枚は5/12試行、4Kの59枚は4/12試行に集中した。内側フレームは表示済み・表示時刻なしの**どちらも全件**で、ウィンドウが`found=1, visible=1, minimized=0, foreground=0`、サイズは1080pが1936×1119、4Kが3856×2180だった。従って最小化・不可視化の切替だけでは今回の未表示を説明できず、非前面であること単独にも相関は見いだせない。ただし背後にある他ウィンドウの遮蔽範囲やDWMの合成判断は観測しておらず、原因を確定したわけではない。PresentModeは全行 `Composed: Flip`。

GStreamer通知間隔p95/p99の試行中央値は1080pで17.81/18.24ms、4Kで17.79/18.30ms。seek後先頭p95中央値3.85/7.26ms、CPU全マシン換算1.13/1.86%、GPU全体使用率平均の試行中央値24.48/35.18%、peak working set中央値61.83/103.21MiB。GPU値は他プロセス込み。通常のGStreamer drop=0と短いPresent API時間だけではOS表示の無欠落を証明できない。

生ログと試行別集計は `results/window-state-{1080p60,4k60}-12*`。前節と同じくPresentMonを別PowerShellで先に起動し、1080pでは以下を実行した。4Kでは素材を`synthetic-2160p60-bt709.mov`、出力名を`4k60`、PresentMonの`--timed`を150秒へ変更する。

```powershell
& 'build/vs18/diagnostic-tools/PresentMon-2.6.0-x64.exe' --process_name d3d11_display_bench.exe --output_file results/window-state-1080p60-12.csv --qpc_time_ms --write_display_metadata --timed 145 --terminate_after_timed --no_console_stats --session_name ProResWinState1080x12
python scripts/benchmark-d3d11-display.py build/vs18/synthetic-1080p60-bt709.mov --loops 3 --repeats 12 --native-rgb --trace-sink-return --trace-window-state --out results/window-state-1080p60-12-bench
python scripts/summarize-presentmon-display.py results/window-state-1080p60-12.csv results/window-state-1080p60-12-bench --out results/window-state-1080p60-12-summary.json
python scripts/summarize-d3d11-display.py results/window-state-1080p60-12-bench --out results/window-state-1080p60-12-bench-summary.json
```

検査器・集計器の変更後に `./scripts/build.ps1`、`./scripts/test-dx11.ps1 -OutDir results/verification-window-state-2026-09-23`、`./scripts/test-d3d11-plugin.ps1 -OutDir results/verification-window-state-2026-09-23` を実行して通過。係数完全一致・YUV最大差1、D3D11Memory 3面UAV、EOS 180枚×3、4位置seek、buffer寿命、6エラー経路、専用RGB EOS/seekを維持した。旧3素材のPresentMon集計は主要数値が変更前と一致し、PID再利用を含む新12試行は各6492行・未照合0・内側捕捉漏れ0を再集計で確認した。`python -m py_compile` と `git diff --check` も通過。表示時刻なし105/59枚は解消していない。

### GPU完了と表示機会の分離

上の同時ログをPTSで再結合し、[PresentMonの定義](https://github.com/GameTechDev/PresentMon/blob/main/README-ConsoleApplication.md#csv-columns) にある `MsRenderPresentLatency`（PresentからそのフレームのGPU作業完了）と `MsUntilDisplayed`（PresentからOS表示）を比較した。各行のQPC `CPUStartQPCTimeInMs + MsCPUBusy` をPresent付近の時刻として加算する**近似**であり、DWMの実際のラッチ締切や光学表示を直接測ったものではない。未表示内側フレームの次のOS表示更新との関係は以下。

| 合成素材 | 表示時刻なし | 次の表示更新を観測 | GPU完了が次の更新より2ms以上前 | 5ms以上前 | GPU完了が次の更新後 | 次の更新のPTSが元より古い |
|---|---:|---:|---:|---:|---:|---:|
| 1080p60 | 105 | 104 | 73 | 31 | 31 | 94 |
| 4K60 | 59 | 59 | 16 | 16 | 43 | 58 |

1080pの残り1枚は試行末付近で次の表示更新がなく比較不可。5ms以上前にGPU完了した47枚を含むので、すべてをDX11計算が遅いだけとは説明できない。一方、次の更新時点でGPU完了していないフレームも31/43枚あり、全件がOSの単独障害とも言えない。多くの次の表示更新はまだ**前のPTS**を表示しており、「直後のフレームに即時追い越された」だけという仮説も合わない。DWMのラッチ締切とflip queueは未観測で、これらの時刻は原因の単独証明ではない。未表示が起きた試行でも`compressed→decoded`中央値は1080p約3.7～4.6ms、4K約6.4～7.0ms、PresentからGPU完了の中央値は1080p約0.7ms、4K約1.7～3.0msで、100ms級の復号停止とは別の事象だった。

[GstBaseSinkの`ts-offset`仕様](https://gstreamer.freedesktop.org/documentation/base/gstbasesink.html#gst_base_sink_set_ts_offset) に基づき、検査器のみ `--sink-ts-offset-ms=-5` で5ms早い同期を試した。公開実写ではなく合成1080p60の別12試行×540枚。GStreamerは6475/6480枚、sink drop 0、1試行でdecoder QoS 5件・復号出力前5枚欠落。内側6403枚中149枚にOS表示時刻がなく、全PresentMon行のPTS照合と内側捕捉漏れ0を確認した。通常同期の105/6408枚より良くなく、sink push最大34.62ms、OS最大間隔50.02ms。前倒し試行のうち1試行108枚はウィンドウが前面で、他の内側フレームは非前面だった。通常同期との比較は非交互であり、環境変動や前面状態が揃わないため、差を`ts-offset`だけの因果と断定しない。**前倒し設定を製品既定値には採用しない。** 再現用検査器にはsink propertyの読み戻し一致検査も追加し、同設定の短い試験で通過した。生ログは `results/offset-minus5-1080p60-12*`。

```powershell
& 'build/vs18/diagnostic-tools/PresentMon-2.6.0-x64.exe' --process_name d3d11_display_bench.exe --output_file results/offset-minus5-1080p60-12.csv --qpc_time_ms --write_display_metadata --timed 145 --terminate_after_timed --no_console_stats --session_name ProResOffsetM5HD12
python scripts/benchmark-d3d11-display.py build/vs18/synthetic-1080p60-bt709.mov --loops 3 --repeats 12 --native-rgb --trace-sink-return --trace-window-state --sink-ts-offset-ms=-5 --out results/offset-minus5-1080p60-12-bench
python scripts/summarize-presentmon-display.py results/offset-minus5-1080p60-12.csv results/offset-minus5-1080p60-12-bench --out results/offset-minus5-1080p60-12-summary.json
```

この集計器／検査器の変更後も `./scripts/build.ps1`、`./scripts/test-dx11.ps1 -OutDir results/verification-cadence-2026-09-23`、`./scripts/test-d3d11-plugin.ps1 -OutDir results/verification-cadence-2026-09-23` が通過した。合成1080p/4Kの係数完全一致・YUV最大差1、D3D11Memory 3面UAV、EOS・4位置seek・buffer寿命・6エラー経路、専用RGB EOS/seekを維持。3条件のOSログはPID/QPC/PTSで全行照合・内側捕捉漏れ0。`python -m py_compile`、`git diff --check` も通過。実表示の欠落は未解決。

この診断コードを追加した後に `./scripts/build.ps1`、`./scripts/test-dx11.ps1 -OutDir results/verification-display-qos-2026-09-23`、`./scripts/test-d3d11-plugin.ps1 -OutDir results/verification-display-qos-2026-09-23` を実行して通過。合成1080p/4Kの係数一致・YUV最大差1、D3D11Memory 3面UAV、EOS 180枚×3、4位置seek、buffer寿命、6エラー経路、専用RGBの180枚EOS/seekを確認した。`python -m py_compile scripts/benchmark-d3d11-display.py scripts/summarize-d3d11-display.py` と `git diff --check` も通過。診断条件は製品の既定動作を変更せず、表示停止の原因は未確定のままである。

今回の計測器変更後に `./scripts/build.ps1`、`./scripts/test-dx11.ps1 -OutDir results/verification-color-stage`、`./scripts/test-d3d11-plugin.ps1 -OutDir results/verification-color-stage` を実行した。合成1080p/4KのVLD係数完全一致・画素最大差1、D3D11Memoryの3面UAV、EOS 180枚×3、4位置seek、buffer寿命、6エラー経路が通った。公開実写のYUV全50/129枚も再比較しY/U/V最大差1を維持した（同ディレクトリの `proton-yuv.json`、`dji-yuv.json`）。`python -m py_compile` と `git diff --check` も通過。RGBの系統差と表示の稀な欠落は、この回帰検査の合格で解消したことにはならない。

専用RGB shaderを追加した後も `./scripts/build.ps1`、`./scripts/test-dx11.ps1 -OutDir results/verification-native-rgb-2026-09-23`、`./scripts/test-d3d11-plugin.ps1 -OutDir results/verification-native-rgb-2026-09-23` が通った。合成1080p/4Kの係数完全一致・画素最大差1、3面D3D11Memory UAV、EOS 180枚×3、4 seek、buffer寿命、6エラー経路を維持。RTX 3070のRGB10A2 typed UAVも利用可能だった。同ディレクトリで公開実写YUV全50/129枚を固定SDK CPUと再比較し、各面最大差1を維持。専用RGBの全画素検証は上記のJSONを参照。`python -m py_compile` と `git diff --check` も通過した。表示経路の稀な欠落は未解決であり、この検証結果を流用しない。

### DJI公開実写4K60の全フレーム品質と実表示

DJI公式REC.709原本480枚を追加し、固定SDK CPU対DX11のY/U/V全画素で各面最大差1、統合済み `proresd3d11rgb` のD3D11Memory RGB10A2対独立BT.709式でR/G/B最大差1、A完全一致を確認した。前者はCPU復号との一致、後者はGPU色変換の数値検査であり、カメラの未指定クロマ位置や表示機器の色管理まで保証しない。結果は `results/dji-real-4k60-yuv.json` と `results/dji-real-4k60-rgb-formula.json`。比較器の既定一時領域もProton 50枚の回帰で合格した（`results/dji-real-4k60-comparator-regression.json`）。

同じ原本を専用RGB要素からD3D11表示sinkへ3周×3試行した。通常のdecoder QoS有効では4320枚予定の4311枚が表示経路へ到達し、9枚は圧縮入力後・DX11復号出力前に欠けた。QoS通知は計9件、sink drop申告0。診断限定でdecoder QoS破棄を無効化すると4320/4320枚到達、sink drop 0となった。ただし別時刻の非交互試行であり、両条件のOS表示率差を設定だけの因果と断定しない。

| 条件 | GStreamer到達／予定 | present間隔p95/p99中央値 | 最大present間隔 | OS照合できた内側フレームの表示時刻なし | OS最大表示間隔 |
|---|---:|---:|---:|---:|---:|
| 通常QoS | 4311/4320 | 17.948/19.910 ms | 37.297 ms | 44/2861（捕捉十分な2試行、未捕捉0） | 49.949 ms |
| decoder QoS破棄無効・診断用 | 4320/4320 | 17.847/18.356 ms | 25.642 ms | 16/4302（3試行、未捕捉0） | 33.357 ms |

両条件ともPresentMonの全行をQPC/PTSで照合し、表の内側で捕捉漏れ0。通常条件の1試行は内側捕捉漏れが1枚あり、OS集計の分母から除外した。ウィンドウ状態は表示済み・未表示とも可視、非最小化、非前面だった。OS表示時刻なしはGStreamerの9枚欠落とは別に残るので、QoS無効を製品既定へ採用しない。GPU利用率中央値は通常約32.4%、プロセス最大working set中央値約109MiB。詳細なPTS・段階遅延・GPU・PresentMon生ログは `results/dji-real-4k60-display*`、`results/dji-real-4k60-noqos-display*`、`results/dji-real-4k60-presentmon*`、`results/dji-real-4k60-noqos-presentmon*`。バイナリ素材と約32GBの中間RAWはコミットしない。

```powershell
python scripts/compare-d3d11-real.py media/reference-dji-nature-4k60-rec709-hq.mov --mode yuv --out results/dji-real-4k60-yuv.json --scratch-dir E:\prores-dx11-scratch
python scripts/compare-d3d11-real.py media/reference-dji-nature-4k60-rec709-hq.mov --mode rgb_element_formula --out results/dji-real-4k60-rgb-formula.json --scratch-dir E:\prores-dx11-scratch
python scripts/benchmark-d3d11-display.py media/reference-dji-nature-4k60-rec709-hq.mov --loops 3 --repeats 3 --native-rgb --trace-sink-return --trace-window-state --out results/dji-real-4k60-display
python scripts/benchmark-d3d11-display.py media/reference-dji-nature-4k60-rec709-hq.mov --loops 3 --repeats 3 --native-rgb --trace-sink-return --trace-window-state --decoder-no-qos --out results/dji-real-4k60-noqos-display
```

`--scratch-dir` は空き33GiB以上の既存ディレクトリに読み替える。両表示試行では各々PresentMon 2.6.0を `--process_name d3d11_display_bench.exe --qpc_time_ms --write_display_metadata` で同時取得し、`scripts/summarize-presentmon-display.py` で照合した。

### 前面との重なりと検査ウィンドウ最前面の対照

前回のOS未表示はウィンドウが可視・非最小化・非前面だった。`tests/d3d11_display_bench.cpp` に前面ウィンドウとの矩形重なり率をpresentごとに記録する診断を追加した。これは `GetWindowRect` による矩形の交差率で、透明度・他の上位ウィンドウ・DWMの遮蔽領域を表す値ではない。[Windowsの仕様](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-getwindowrect)上、不可視のサイズ変更枠も含み得る。実写4K60をdecoder QoS破棄無効で3周×3試行したところ、表示済み4234枚・OS表示時刻なし68枚の全件で前面ウィンドウ重なり率が23%だった。重なり率の変化は欠落タイミングを分けなかった。

遮蔽仮説の対照として、検査プロセスが作成したD3D11表示ウィンドウだけに `--topmost-window` を指定した。[`SetWindowPos`の`HWND_TOPMOST`と`SWP_NOACTIVATE`](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-setwindowpos)を一度だけ非同期で要求し、前面アプリは切り替えない。実際の `WS_EX_TOPMOST` を各presentで再読出しして確認する。短い試験は481 present中、初回を除く480回で最前面を確認した。3周×3試行では要求成功3/3、最前面を4325 presentで確認。プロセス終了時に検査ウィンドウは破棄され、他アプリのウィンドウ設定は変更しない。

| 実写4K60・decoder QoS破棄無効 | GStreamer到達／予定 | sink drop | present間隔p95/p99中央値 | OS照合内側の表示時刻なし | OS最大表示間隔 |
|---|---:|---:|---:|---:|---:|
| 前面重なり記録のみ、通常ウィンドウ | 4320/4320 | 0 | 17.786/18.452 ms | 68/4302（3試行、未捕捉0） | 49.961 ms |
| 検査ウィンドウ最前面 | 4319/4320 | 1 | 17.708/18.172 ms | 38/4301（3試行、未捕捉0） | 48.516 ms |

最前面試行のsink drop 1枚は、圧縮入力・DX11復号・RGB出力・sink pushの各段階には1440枚到達した後に発生した。OS表示時刻なし38枚はすべて最前面フラグ付きで、うち36枚は次の表示更新より5ms以上前にGPU作業が完了した。したがって通常ウィンドウより上位の非最前面ウィンドウによる遮蔽だけでは、この欠落を説明できない。ただし他の最前面ウィンドウやDWMの合成・ラッチは未観測で、2条件は非交互なので欠落率差を最前面化の効果とは断定しない。最前面化も製品既定には採用しない。生ログと集計は `results/dji-real-4k60-overlap-*`、`results/dji-real-4k60-topmost-*` に保存した。古いPresentMonログの再集計結果は変更前とSHA256が一致。`./scripts/build.ps1`、`./scripts/test-dx11.ps1`、`./scripts/test-d3d11-plugin.ps1`、Python構文検査と`git diff --check`も通過した。

最前面対照の再現例（通常ウィンドウでは `--topmost-window` を外して出力先を分ける）：

```powershell
$pm = Start-Process -FilePath 'build/vs18/diagnostic-tools/PresentMon-2.6.0-x64.exe' -ArgumentList @('--process_name','d3d11_display_bench.exe','--output_file','results/dji-real-4k60-topmost-presentmon.csv','--qpc_time_ms','--write_display_metadata','--timed','115','--terminate_after_timed','--no_console_stats','--session_name','ProResDJITopmost') -WindowStyle Hidden -PassThru
python scripts/benchmark-d3d11-display.py media/reference-dji-nature-4k60-rec709-hq.mov --loops 3 --repeats 3 --native-rgb --trace-sink-return --trace-window-state --decoder-no-qos --topmost-window --out results/dji-real-4k60-topmost-display
Wait-Process -Id $pm.Id
python scripts/summarize-d3d11-display.py results/dji-real-4k60-topmost-display --out results/dji-real-4k60-topmost-display-summary.json
python scripts/summarize-presentmon-display.py results/dji-real-4k60-topmost-presentmon.csv results/dji-real-4k60-topmost-display --out results/dji-real-4k60-topmost-presentmon-summary.json
```

### 復号器を外したD3D11表示対照

[PresentMonの`MsUntilDisplayed`定義](https://github.com/GameTechDev/PresentMon/blob/main/README-ConsoleApplication.md#csv-columns)では、値がある行はそのPresentが表示された時刻を持つ。実写4K60の未表示がsink／DWMだけで必ず生じるのかを分けるため、固定GStreamer 1.28.2の `d3d11testsrc` から同じ `d3d11videosink sync=true qos=true` へ、RGB10A2 D3D11Memoryを3840×2160/60fps・1440枚ずつ3プロセス送った。各プロセスはEOS成功・約24.4～24.6秒。PresentMonを`gst-launch-1.0.exe`で捕捉し、`scripts/summarize-presentmon-reference.py`でPID別に集計した。捕捉は各1439行、各先頭末尾3行を除く4299行にOS表示時刻なし0。全行`Composed: Flip`、GPU busy p99は各0.69～0.83ms、present間隔p99は18.20～18.31msだった。

次に復号器を外し、RGB10A2 D3D11Memoryのテストソース→標準GPU `d3d11convert`でI422_10LE→専用`proresd3d11rgb`→同じsinkを通した。各1440枚・3プロセスともEOS成功。PresentMon捕捉は1439/1436/1439行、内側4296行で表示時刻なし1。全行`Composed: Flip`、GPU busy p99は1.16～1.32ms、present間隔p99は18.22～18.37msだった。これらの対照はProRes経路の通常ウィンドウ3試行の未表示68/4302件や最前面3試行の38/4301件より少ない。ただし実行時刻・ウィンドウ状態・GPU負荷は一致せず、PTS照合もsink rendered/drop統計も対照にはないため、この差をデコーダー単独の原因と断定しない。少なくとも同じsinkとDWMが単純なGPU入力で必ず未表示を起こすわけではない。ProResの最前面試行ではGPU busy p99約6.3～6.6msだが、未表示フレーム自身のPresent→GPU完了中央値は表示済みフレームより大きくなかった。DWMラッチ締切やGPUキュー内の位置はなお未観測。

直接I422_10LEを出す`d3d11testsrc`を`proresd3d11rgb`へつないだ最初の試行は安全に拒否された。`d3d11testsrc`の面0はR16_UNORM・配列数1・3840×2160だがbind flagが160（UAVとRTVのみ）で、shader resource用flagがない。RGB要素は3面R16_UNORMにSRVを要求するため、エラーへformat・配列数・寸法・bind flag・subresourceを追加した。現行のProResデコーダー出力はSRVありで、外部I422 D3D11Memoryの一般受け入れは保証しない。標準GPU変換は**対照試験だけ**に使い、製品のDX11復号経路へ追加していない。変更後も`./scripts/build.ps1`、`./scripts/test-d3d11-plugin.ps1`、公開Proton全50枚の専用RGB対独立BT.709式でR/G/B最大差1・A一致、Python構文検査と`git diff --check`が通過した。

```powershell
& 'C:\Program Files\gstreamer\1.0\msvc_x86_64\bin\gst-launch-1.0.exe' -q -e d3d11testsrc num-buffers=1440 '!' 'video/x-raw(memory:D3D11Memory),format=RGB10A2_LE,width=3840,height=2160,framerate=60/1' '!' d3d11videosink sync=true qos=true
$env:GST_PLUGIN_PATH = (Resolve-Path 'build/vs18/plugins/Release').Path
& 'C:\Program Files\gstreamer\1.0\msvc_x86_64\bin\gst-launch-1.0.exe' -q -e d3d11testsrc num-buffers=1440 '!' 'video/x-raw(memory:D3D11Memory),format=RGB10A2_LE,width=3840,height=2160,framerate=60/1,colorimetry=bt709' '!' d3d11convert '!' 'video/x-raw(memory:D3D11Memory),format=I422_10LE,colorimetry=bt709,chroma-site=jpeg' '!' proresd3d11rgb '!' d3d11videosink sync=true qos=true
python scripts/summarize-presentmon-reference.py results/d3d11testsrc-4k60-presentmon.csv --expected-frames 1440 --expected-trials 3 --out results/d3d11testsrc-4k60-summary.json
python scripts/summarize-presentmon-reference.py results/d3d11testsrc-converted-rgb-4k60-presentmon.csv --expected-frames 1440 --expected-trials 3 --out results/d3d11testsrc-converted-rgb-4k60-summary.json
```

各gst-launch行は3回実行し、その間はPresentMon 2.6.0を`--process_name gst-launch-1.0.exe --qpc_time_ms --write_display_metadata --timed 105`（RGB変換ありは110秒）で捕捉する。生ログ・CSV・集計JSONは `results/d3d11testsrc-4k60-*` と `results/d3d11testsrc-converted-rgb-4k60-*`。一時的な環境変数だけを使い、システムのPATHやFFmpegは変更していない。

GPU負荷とDWMの関係を探るため、復号器なしのまま、RGB→I422→RGB→I422→RGB→I422の5回の標準GPU変換後に専用RGB要素を通した。4K60・1440枚の単発ではPresentMon内側1433行中69行にOS表示時刻なし、GPU busy p99は約2.00ms、Present→GPU完了p99は約2.07ms。**同じ構成**の追加3試行では各0/1433、0/1433、11/1433で大きく変動した。4試行とも約24.4～24.6秒でEOSまで完走し、PresentMonは各1439行を捕捉した。最初の69件では未表示行のPresent→GPU完了中央値1.17msに対し表示済みは1.20msで、未表示行だけが一律に遅いわけではなかった。これで復号器がなくてもOS未表示は生じ得ると分かったが、変換回数・GPU時間・ウィンドウ状態がProRes経路と同一ではなく、DWMのラッチ締切も未測定。ProResデコーダーの無罪や色変換の有罪を断定しない。対照のsink rendered/dropおよびPTS別捕捉も未取得。記録は `results/d3d11testsrc-heavy-4k60-*` と `results/d3d11testsrc-heavy-repeat-4k60-*`。

同じ対照を既存の`d3d11_display_bench`へ診断用入力モードとして追加し、ProRes表示と同じsink統計・段階別PTS・`present`通知・QPCを取得した。`testsrc-rgb`は4K60 RGB10A2の直接入力、`testsrc-heavy`は上記の5回GPU変換と専用RGB要素を通す。いずれも1440枚×3試行、単周、通常QoS・通常ウィンドウ。各試行の`generated`、`rgb`、sink push／復帰は全て1440行で、sink render合計4320、drop 0、QoS 0。PresentMonは各試行1439行、QPCとPTSは両条件とも各4317行すべて1ms以内で照合され、各条件の内側4314枚は捕捉漏れ0・OS表示時刻なし0。直接入力の表示間隔p99中央値16.7772ms／最大21.6751ms、変換負荷ありは16.8092ms／最大21.6699ms。PresentMon GPU busy p99は直接入力0.81～0.97ms、変換負荷あり1.63～1.87ms。`Composed: Flip`で、表示中の自ウィンドウは可視・非最小化・非前面だった。最初の直接入力スモーク単発はsink render1437、drop／QoS各3であり、対照も常に無欠落ではない。このスモークにはPresentMonを付けていない。

新しい3試行では高負荷対照のOS未表示が再現しなかった。以前の69/1433、11/1433と矛盾ではなく、実行時刻・GPUキュー・DWM位相など未制御条件による変動が残る。ProRes実写4K60のQoSなし通常ウィンドウでの68/4302とは、PTS照合精度とsink統計を揃えたが、GPU busy p99約6msの負荷と入力経路はなお異なる。今回の0件をもって高負荷対照や製品の表示問題が解消したとは扱わない。生ログは`results/reference-display-{rgb,heavy}*`、試行別のPTS照合は各`*-summary.json`。固定SDKのProRes製品経路には変更を加えていない。

```powershell
$pm = Start-Process -FilePath 'build/vs18/diagnostic-tools/PresentMon-2.6.0-x64.exe' -ArgumentList @('--process_name','d3d11_display_bench.exe','--output_file','results/reference-display-heavy-presentmon.csv','--qpc_time_ms','--write_display_metadata','--timed','90','--terminate_after_timed','--no_console_stats','--session_name','ProResReferenceHeavy') -WindowStyle Hidden -PassThru
python scripts/benchmark-d3d11-reference.py --mode heavy --repeats 3 --out results/reference-display-heavy
Wait-Process -Id $pm.Id
python scripts/summarize-presentmon-display.py results/reference-display-heavy-presentmon.csv results/reference-display-heavy --out results/reference-display-heavy-summary.json
```

直接入力は`heavy`を`rgb`へ読み替える。PresentMon開始からベンチ開始まで2秒待ち、計測は90秒で停止した。変更後のビルド、DX11係数／画素／D3D11Memory検査、プラグインEOS・seek・寿命・エラー経路検査、Python構文検査は通過した（`results/verification-reference-*`）。

高負荷対照の再現例（`$env:GST_PLUGIN_PATH` は上記と同じ、各試行で`num-buffers=1440`）：

```powershell
& 'C:\Program Files\gstreamer\1.0\msvc_x86_64\bin\gst-launch-1.0.exe' -q -e d3d11testsrc num-buffers=1440 `
  '!' 'video/x-raw(memory:D3D11Memory),format=RGB10A2_LE,width=3840,height=2160,framerate=60/1,colorimetry=bt709' `
  '!' d3d11convert '!' 'video/x-raw(memory:D3D11Memory),format=I422_10LE,colorimetry=bt709,chroma-site=jpeg' `
  '!' d3d11convert '!' 'video/x-raw(memory:D3D11Memory),format=RGB10A2_LE,colorimetry=1:1:5:1' `
  '!' d3d11convert '!' 'video/x-raw(memory:D3D11Memory),format=I422_10LE,colorimetry=bt709,chroma-site=jpeg' `
  '!' d3d11convert '!' 'video/x-raw(memory:D3D11Memory),format=RGB10A2_LE,colorimetry=1:1:5:1' `
  '!' d3d11convert '!' 'video/x-raw(memory:D3D11Memory),format=I422_10LE,colorimetry=bt709,chroma-site=jpeg' `
  '!' proresd3d11rgb '!' d3d11videosink sync=true qos=true
```

### GPU負荷を近づけた復号器なし対照とPresent方式

`d3d11_display_bench`に診断専用の`testsrc-stress`を追加した。4K60 RGB→I422→RGBを11往復し、さらにI422へ変換して専用RGB要素へ渡す。標準GPU変換23回＋専用RGB1回で、ProRes復号器は含まない。4K60・1440枚×3試行では各試行で`generated`、`rgb`、sink push／復帰のPTSを1440件、sink renderを1440件記録し、drop/QoSは0。PresentMonのGPU busy p99は3.78～4.06msで、実写ProResの5.90～6.31msには達しない。したがって処理負荷を同一にした対照ではない。

PresentMon捕捉は1437/1439/1439行。先頭試行は`present`通知1441件に対し捕捉が4件少なく、内側PTSにも1件の捕捉漏れがあるため、OS欠落率の正式集計から除外した。十分捕捉できた残り2試行は全行をQPCとPTSで1ms以内に照合し、内側各1438枚のOS表示時刻なしが**139枚／0枚**と大きく変動した。139枚の試行はOS最大表示間隔33.85ms、表示済みフレームのGPU busy p99 3.78ms、未表示フレーム自身のGPU busy p99 3.68ms。139枚は全件、検査ウィンドウが可視・非最小化・非前面で、矩形重なり率23%だった。次のOS表示更新より5ms以上前にGPU完了したものも17枚ある。復号器なしでも、sink renderの欠落と別にOS表示時刻なしが起きることを、同じPTS・sink統計で再確認した。ただし未表示0枚の試行もあり、GPU負荷だけを原因とは断定しない。生記録は`results/reference-display-stress*`、回帰検査は`results/verification-reference-stress*`。

この環境のProRes実写・軽量対照・上記負荷対照のPresentMon行はすべて`SyncInterval=0`かつ`Composed: Flip`だった。[GStreamer 1.28.2のWin32実装](https://raw.githubusercontent.com/GStreamer/gstreamer/1.28.2/subprojects/gst-plugins-bad/sys/d3d11/gstd3d11window_win32.cpp)も`IDXGISwapChain1::Present1(0, present_flags, ...)`、または`IDXGISwapChain::Present(0, present_flags)`を呼ぶ。[MicrosoftのDXGI仕様](https://learn.microsoft.com/en-us/windows/win32/api/dxgi/nf-dxgi-idxgiswapchain-present)では、flip modelのsync interval 0は、より新しいフレームがキューに入ったとき古いフレームを捨て得る。GStreamerの`sink sync=true`はPTSに対するクロック同期であり、DXGIのsync intervalを1にする設定ではない。これはOS未表示の**可能な機構**を示すが、今回の139枚すべてがこの規則で捨てられたという個別のETW証拠はまだない。製品側の同期やQoS既定値は変更しない。

```powershell
$pm = Start-Process -FilePath 'build/vs18/diagnostic-tools/PresentMon-2.6.0-x64.exe' -ArgumentList @('--process_name','d3d11_display_bench.exe','--output_file','results/reference-display-stress-presentmon.csv','--qpc_time_ms','--write_display_metadata','--timed','95','--terminate_after_timed','--no_console_stats','--session_name','ProResReferenceStress') -WindowStyle Hidden -PassThru
Start-Sleep -Seconds 2
python scripts/benchmark-d3d11-reference.py --mode stress --repeats 3 --out results/reference-display-stress
Wait-Process -Id $pm.Id
python scripts/summarize-presentmon-display.py results/reference-display-stress-presentmon.csv results/reference-display-stress --out results/reference-display-stress-summary.json
```

### DXGI ETWとPTS照合：Present APIの成功とOS表示は別

Windows標準WPRのGPU/Videoプロファイルは、この環境ではシステム性能プロファイル権限エラー`0xc5585011`で開始できなかった。一方、`logman`でDXGI、DWM-Core、DxgKrnlの個別ETWプロバイダを記録できた。DXGI ID42（Present開始）・ID43（終了）を`scripts/summarize-dxgi-etw.ps1`でPID・順序・`present`通知との相対時刻1ms以内で照合した。生ETLは容量のある`E:\prores-dx11-etw-20260923\`へ置き、リポジトリには含めない。

復号器なし`testsrc-stress`の追加3試行は各sink render1440/1440、drop/QoS 0。各試行で`present`通知・DXGI開始・DXGI終了が**各1441件**、単一swap chain、sync interval 0、flags 0、DXGI結果`S_OK`。通知とDXGI開始の最大相対時刻ずれは0.131/0.255/0.205ms。PresentMon十分捕捉の2試行では内側OS表示時刻なしが0/1438、90/1438だった。残る1試行は内側2枚の捕捉漏れにより正式なOS欠落率から除外した。DXGI API成功はOS表示を意味しないことを、同じPTS系で確認した。`results/reference-display-stress-etw*`を参照。DXGI ETLのSHA256は`6757955f3c159a2ef08d14183a63fedfe32d46a8a23ee06d305e0a1e3764b79a`。同時取得したDWM-Core/DxgKrnl ETLのSHA256は`d13c3f33fc7b87733f0c51843699260c81861ae172d1d0d039aee71eb70c32cf`／`3b37d4aee3306e11dd3b8252f9eb979b58d66ed81bb9cc05b3e3c4cc2d3d4859`。これらのDWM/カーネルイベントと個々のPTSとの対応は未確定で、キュー破棄理由をまだ特定していない。

公開DJI実写4K60も同じ純粋DX11→専用RGB→標準sinkを3周×3試行した。decoder出力・sink renderは各1440/1440、drop/QoS 0（診断用decoder QoS破棄無効）。各試行の`present`通知・DXGI開始・終了は**各1443件**で、全件sync interval 0、flags 0、DXGI結果`S_OK`、最大相対時刻ずれ0.080/0.084/0.072ms。PresentMon十分捕捉の2試行では内側OS表示時刻なし105/1434、20/1434、合計125/2868。先頭試行は内側1枚の捕捉漏れがあり正式集計から除外した。単発の先行試行もDXGI全1443件成功だがPresentMon捕捉不足だったため、欠落率の判断には使わない。`results/dji-real-4k60-etw*`、反復DXGI ETLのSHA256`578176ffe21c0c7244640d3c070a132e8c9c77338e0057718a88732de9985447`。いずれもDXGI呼び出し失敗を欠落原因とはできないが、`Present(0)`のキュー破棄とDWMラッチのどちらで落ちたかはなお未証明。

再現時はPresentMonを先に起動し、以下のDXGI記録でベンチを挟む。3試行ではPresentMonの`--timed 100 --set_circular_buffer_size 8192`を使い、終了後に`scripts/summarize-presentmon-display.py`でPTS照合した。DXGI ETWの出力先は空き容量のある一時領域へ読み替える。`logman`セッションは失敗時も`finally`で停止する。

```powershell
logman start ProResRealDxgiRepeat -ets -o 'E:\prores-dx11-etw-20260923\prores-dxgi-repeat.etl' -p Microsoft-Windows-DXGI 0x7 0x5
$pm = $null
try {
  $pm = Start-Process -FilePath 'build/vs18/diagnostic-tools/PresentMon-2.6.0-x64.exe' -ArgumentList @('--process_name','d3d11_display_bench.exe','--output_file','results/dji-real-4k60-etw-repeat-presentmon.csv','--qpc_time_ms','--write_display_metadata','--set_circular_buffer_size','8192','--timed','100','--terminate_after_timed','--no_console_stats','--session_name','ProResRealETWRepeat') -WindowStyle Hidden -PassThru
  Start-Sleep -Seconds 2
  python scripts/benchmark-d3d11-display.py media/reference-dji-nature-4k60-rec709-hq.mov --loops 3 --repeats 3 --native-rgb --trace-sink-return --trace-window-state --decoder-no-qos --out results/dji-real-4k60-etw-repeat-display
} finally {
  logman stop ProResRealDxgiRepeat -ets
}
Wait-Process -Id $pm.Id
python scripts/summarize-presentmon-display.py results/dji-real-4k60-etw-repeat-presentmon.csv results/dji-real-4k60-etw-repeat-display --out results/dji-real-4k60-etw-repeat-presentmon-summary.json
./scripts/summarize-dxgi-etw.ps1 -EtlPath 'E:\prores-dx11-etw-20260923\prores-dxgi-repeat.etl' -BenchDir results/dji-real-4k60-etw-repeat-display -PresentMonSummary results/dji-real-4k60-etw-repeat-presentmon-summary.json -Out results/dji-real-4k60-etw-repeat-dxgi-summary.json
```

### DWMのflip消費をPresentMon行へ照合

標準`d3d11videosink`のDXGI Present成功後、どの段階でOS未表示となるかを狭めるため、DWM-Core ID467 `Windowed_Dx_Flip_Consumed`の対象HWND・`presentCount`を、DXGI ID42のPID・時刻、PresentMonの同一PID行へ照合した。`scripts/summarize-dwm-flip-consumed.ps1`は当該swap chainの`presentCount`がPresentMon行番号より2大きいことを明示指定し、DXGI→DWM消費の順序と0～100msの遅延、DXGI↔PresentMonの相対時刻最大2ms、重複countなしを確認する。境界に捕捉漏れがあれば一致率を出さない。

| 入力・試行 | GStreamer sink到達 | PresentMonのOS表示時刻なし | DWM flip消費欠番 | 行別一致 |
|---|---:|---:|---:|---:|
| 復号器なしGPU負荷対照、捕捉十分2試行 | 各1440/1440 | 0、90／各1439行 | 0、90／各1439 count | 各1439/1439 |
| DJI実写4K60、decoder QoS破棄無効、3試行 | 各1440/1440、sink drop/QoS各0 | 22、3、7／各1441行 | 22、3、7／各1441 count | 各1441/1441 |

実写の3試行はDXGI Present開始／終了が各1443件、全件`sync interval=0`・flags=0・`S_OK`。PresentMonのPTS内側ではOS表示時刻なし23/4302枚、捕捉漏れ0、最大表示間隔33.38ms。試行別内側は19・0・4枚で、上表の22・3・7件には各ループ境界のPresentも含む。DXGI↔PresentMon相対時刻の最大差は1.10/1.85/0.42ms、消費イベントは対応するDXGI開始より全件後だった。復号器なし対照の初回はPresentMon捕捉不足のため行別一致率から除外した。

この一致は「DXGI成功だが、対象のflipをDWMが消費した記録はない」ことを個別presentへ帰属する。DWMが自ら破棄したのか、DWMに届く前にflip queueで置換されたのかは区別できない。またPresentMonと本照合は同じOS ETW系を使い、独立した光学表示計測ではない。[PresentMon 2.6.0の追跡実装](https://github.com/GameTechDev/PresentMon/blob/v2.6.0/PresentData/PresentMonTraceConsumer.cpp)はWin32K tokenとDWM surface updateを追跡しており、ID467だけを読み出して表示判定する実装ではないが、完全に独立した証拠として二重に数えない。

実写の生CSV・集計は`results/dji-real-4k60-dwm-2026-09-24/`、復号器なし対照の追加集計は`results/reference-display-stress-etw-dwm-summary.json`。実写の生ETLは容量のある`E:\prores-dx11-etw-20260924\`へ非コミット保存（DXGI SHA256 `7f5a6866ca10a6998ef594290c21da7044122e1745657617ddc620379b9f617a`、DWM SHA256 `92c5a452d607c8dccba9e35f664e7989bb39b4e00b6aa1e30e8296206053a8f2`）。再取得は前節のDXGI・PresentMon同時捕捉に`logman start ProResDwmReal24 -ets -o 'E:\prores-dx11-etw-20260924\real-dwm.etl' -p Microsoft-Windows-Dwm-Core 0x1 0x5`を追加し、終了後`logman stop ProResDwmReal24 -ets`。ベンチは`python scripts/benchmark-d3d11-display.py media/reference-dji-nature-4k60-rec709-hq.mov --loops 3 --repeats 3 --native-rgb --trace-sink-return --trace-window-state --decoder-no-qos --out results/dji-real-4k60-dwm-2026-09-24`。`PresentMon --timed 105 --set_circular_buffer_size 8192`で取得し、`scripts/summarize-presentmon-display.py`、`scripts/summarize-dxgi-etw.ps1`と新集計器に`-PresentCountOffset 2`を渡す。固定FFmpeg SDK以外は使わず、プラグイン・GStreamer SDKのコードと製品設定は変更していない。

### Win32K token状態：DWM未消費の2種類

同じDJI実写4K60を3試行×3周、decoder QoS破棄無効・標準`d3d11videosink`で再測定した。DXGI Presentは各1443件・全件`S_OK`、GStreamer sinkは各1440/1440枚・drop/QoS 0。PresentMonは3試行ともPTS内側を欠測なく捕捉し、OS表示時刻なし120/4302枚だった。全PresentMon行では13/8/108件、DWM flip消費欠番も同じ13/8/108件で、各1441行の状態が完全一致した。Win32K ID201/301をDWM ID467の`CompositionSurfaceLuid`・`BindId`・`presentCount`へ結合すると、各試行1443 tokenの生成と、境界を除く1441 tokenの状態を全件追跡できた。[PresentMon 2.6.0のWin32K定義](https://github.com/GameTechDev/PresentMon/blob/v2.6.0/PresentData/ETW/Microsoft_Windows_Win32k.h)の状態値2/3/4/5/6は順にCompleted/InFrame/Confirmed/Retired/Discardedである。

| 試行 | DWM未消費 | 未消費で`2→3→4→5→6` | 未消費で`2→3→4→6` | 次の消費tokenとInFrame時刻差≤0.1ms | 消費済み隣接組の同条件 |
|---|---:|---:|---:|---:|---:|
| 0 | 13 | 4 | 9 | 4/13 | 0/1415 |
| 1 | 8 | 5 | 3 | 5/8 | 0/1425 |
| 2 | 108 | 108 | 0 | 108/108 | 0/1225 |
| 合計 | **129** | **117** | **12** | **117/129** | **0/4065** |

117件のInFrame時刻差は最大0.0139msで、次に消費されたtokenとほぼ同時の合成処理に入っていた。これに対し`Retired`状態5のない12件は、次の消費tokenのInFrameまで16.6494～16.773ms離れた。消費済み隣接組のInFrame時刻差中央値は約16.67msで、0.1ms以内の組はない。全件が状態3/4まで到達し、`IndependentFlip`と`EarlyComposition`は全件false。状態6は表示済みtokenにも出るので、それ単独を「表示前の破棄」と読まない。117件は同じ合成タイミングで後続tokenに置き換わった可能性が高いが、Win32KとDWMの記録だけで正確な破棄主体・キュー位置は断定できない。

再現集計は`results/dji-real-4k60-token-2026-09-24/`と`scripts/summarize-win32k-present-tokens.ps1`。前節のDXGI・DWM同時記録に、`logman start ProResTokWin24 -ets -o 'E:\prores-dx11-etw-20260924-token\real-win32k.etl' -p Microsoft-Windows-Win32k 0x1000 0x5`と`logman start ProResTokKrnl24 -ets -o 'E:\prores-dx11-etw-20260924-token\real-dxgkrnl.etl' -p Microsoft-Windows-DxgKrnl 0x8000001 0x5`を追加し、各sessionを`finally`で停止した。PresentMonは`--timed 110 --set_circular_buffer_size 8192`、表示ベンチは前節と同じ引数。生ETLはEドライブへ非コミット保存し、SHA256はWin32K `6f399f610789a36e881d8b678600fbb8e132ca7033e40749386533945ec29895`、DWM `54ca1fd5b37fcc924aef29c75e10c99365ce8122dff7fb97aea39c5576d7bb84`、DXGI `3305a497f85c6a63af35ea2837385f1c04fd65d8e4cdb966e5d9db2716579376`、DxgKrnl `2b9a55048725cf28cc7226a8c3fed44cd9621cae48b2a115d6b087d4e3774a5a`。DxgKrnlのPresentHistoryイベントも捕捉したが、各Win32K tokenへの厳密な帰属は未完了で、このETLだけを原因証明に使わない。プラグインとGStreamer SDKの実装・設定は不変。

### DxgKrnl PresentHistory：未表示tokenのDWM handoff前後

上記の同時ETLを`Win32K ID201`と`DxgKrnl ID215 (PresentHistoryDetailed_Start)`で結合した。各試行1443件は同一PID・スレッド・順序で1対1、開始時刻差の最大は0.121/0.116/0.129ms。DxgKrnlの`Token` pointerは再利用されるため、各ID215から同じpointerの次のID215までに出た最初の`ID172 (PresentHistory_Info)`だけを対応させた。[PresentMon 2.6.0実装](https://github.com/GameTechDev/PresentMon/blob/v2.6.0/PresentData/PresentMonTraceConsumer.cpp#L790-L818)ではID172をDWMへ渡せるready時点として扱う。

| 実写4K60試行 | Win32K・DxgKrnl開始の結合 | DWM未消費tokenのID172あり | ID172がWin32K InFrameより前 |
|---|---:|---:|---:|
| 0 | 1443/1443 | 13/13 | 13/13 |
| 1 | 1443/1443 | 8/8 | 8/8 |
| 2 | 1443/1443 | 108/108 | 108/108 |
| 合計 | **4329/4329** | **129/129** | **129/129** |

未表示以外の4200件にも全件ID172があるが、この4200件には各試行の未判定境界2件を含むため全件を「表示済み」とは呼ばない。未表示tokenの開始→readyのp99は各1.583/1.916/1.890msで、未表示以外の1.954/1.943/1.774msと桁が同じ。試行2ではready→InFrame中央値が未表示16.297ms、未表示以外8.232ms。`Retired`まで進んだ未表示117件と、そこを経ない12件は異なる時系列であり、後者のready→InFrame中央値は試行0/1で3.418/5.806ms。これにより未表示129件を「DxgKrnl ready以前で止まった」と説明することはできない。ただしID172は実表示でもDWM消費でもなく、正確な破棄主体・キュー位置までは証明しない。

再現集計は`scripts/summarize-dxgkrnl-present-history.ps1`と`results/dji-real-4k60-token-2026-09-24/dxgkrnl-present-history-summary.json`。入力ETLのSHA256は前節と一致し、出力には全件の照合数・時刻差・状態別分布を残した。素材・SDK・製品プラグインは変更していない。

### 未表示tokenと直後のPresentの時刻順

同じETLのWin32K ID201（token作成）とID301（`InFrame`）を、同一surface/bind/presentCountで再結合した。未表示tokenが`InFrame`へ入る時点で、直後のpresentCountのtokenが既に作られていたかを全129件で検査し、表示済みの隣接4065組を対照にした。DXGI/DWM/PresentMonの帰属、入力ETLのSHA256は前節と同じ。再実行は`scripts/summarize-win32k-present-tokens.ps1`、個別presentCountと符号付き時差は`results/dji-real-4k60-token-2026-09-24/win32k-summary.json`。

| 試行 | 未表示・Retiredあり：直後tokenが先に作成 | 未表示・Retiredなし：直後tokenが後に作成 | 表示済み隣接組：直後tokenが先に作成 |
|---|---:|---:|---:|
| 0 | 4/4 | 9/9 | 0/1415 |
| 1 | 5/5 | 3/3 | 0/1425 |
| 2 | 108/108 | 0/0 | 41/1225 |
| 合計 | **117/117** | **12/12** | **41/4065** |

`直後token作成 − 未表示token InFrame`の時差は、Retiredあり117件では−16.248～−1.474ms、Retiredなし12件では+8.766～+13.085ms。前者は後続Presentが既に提出された状態で合成段階に入り、キュー内で追い越され得る時系列を示す。後者は`InFrame`時点には直後Presentが未提出で、同じ早期追い越しでは説明できない。表示済みでも41組は後続tokenが先に存在したため、先行作成だけを破棄の十分条件としない。ID201/301の時刻順は実際のswap chainキュー位置、DWMの採否理由、光学表示を直接記録しない。同期値変更などの対策候補は、両群・通常QoS・最大表示間隔を別々に再測定してから判断する。

### RGB後queueによるsink分離の交互診断

PTS時計同期を維持した標準`d3d11videosink`で、専用RGB出力とsinkの間に4 bufferの非leaky queueを置く診断を追加した。`scripts/benchmark-display-postrgb-queue.ps1`はqueueなし→あり、あり→なしの順で、DJI実写4K60を各480枚×3周、通常decoder QoS、preroll、EOS後150ms保持で比較する。PresentMonはPID/QPC/PTSで照合し、内側の捕捉漏れ0を各試行で確認した。`Present`同期値0、sink `sync=true`、sink QoS有効は全試行同じ。製品プラグイン・GStreamer SDKは変更していない。

| 試行順 | RGB後queue | GStreamer出力/予定 | QoS破棄 | 内側OS表示時刻なし/捕捉 | 最大OS表示間隔 |
|---|---|---:|---:|---:|---:|
| 0 | なし | 1440/1440 | 0 | 8/1434 | 38.299ms |
| 1 | あり | 1440/1440 | 0 | 65/1434 | 33.759ms |
| 2 | あり | 1440/1440 | 0 | 1/1434 | 33.343ms |
| 3 | なし | 1440/1440 | 0 | 5/1434 | 33.337ms |

queueありの2試行ではRGB出力とqueue出口が各1440枚で完全一致し、queue内待機の最大は78.20/78.00ms。GStreamer側の欠落はこの4試行では生じなかったが、OS未表示はqueueの有無にかかわらず残り、queueありの試行間変動は65→1枚と大きい。直前のPresentMonなし単発ではqueueなしが1439/1440枚（decoder QoS 1）、queueありが1440/1440枚だったが、これもqueueの改善効果を示すには不足する。queueを製品既定経路へは採用しない。記録は`results/postrgb-queue-2026-09-24/`と`results/postrgb-queue-presentmon-2026-09-24/`。PresentMonはOS ETW由来で、光学表示を直接測ったものではない。

### 標準sinkの処理期限を変えた交互診断

非公開ABI hookやSDK改変を使わず、標準`d3d11videosink`の公開`processing-deadline`だけを既定15msと45msで交互に変更した。DJI実写4K60を各480枚×3周、専用RGB、`sync=true`、通常decoder/sink QoS、`Present1(0)`、preroll、検査窓を一時最前面、EOS後150ms保持で測定した。順序は15→45→45→15を2組。PresentMonのPID/QPC/PTS照合は8試行すべて捕捉十分、内側捕捉漏れ0。GStreamer出力は各1440/1440枚、QoS／sink dropは全件0。

| 処理期限 | 4試行の内側OS表示時刻なし | 試行別 | 最大OS表示間隔 |
|---|---:|---|---|
| 15ms | 220/5736枚 | 28、92、56、44枚 | 33.40～34.71ms |
| 45ms | 29/5736枚 | 5、0、7、17枚 | 21.65～33.36ms |

4組とも45ms側の未表示が少ないが、3試行で未表示が残り、最大間隔も約33msのままである。処理期限は表示位相やパイプラインlatencyも変え得るため、今回の差だけで破棄主体や恒久的な最適値は確定しない。24fps実写129枚の別試行は両設定とも129/129枚、wall約5,334ms、GStreamer present間隔中央値約41.6msで、時計同期を外した際の約2.52倍速化は起きなかった。24fpsではPresentMonを取得していない。再現スクリプトは`scripts/benchmark-display-processing-deadline.ps1`、行別記録は`results/display-processing-deadline-2026-09-24/`、`results/display-processing-deadline-confirm-2026-09-24/`、24fpsは`results/display-processing-deadline-24fps-2026-09-24/`。最前面以外、別GPU、長時間、通常QoSで欠落が起きた試行への効果は未検証であり、45msを製品既定値へ採用しない。PresentMonはOS ETW由来で、光学表示を直接測らない。

## 次の手順

1. 実写Protonの自然停止は別試行で`present`通知前約201ms、通知後約188msと位置が異なる。PresentMonではPresent API本体の長時間待ちは確認されなかった。DJI実写のDWM未消費129件は全件DxgKrnl ready後にWin32K InFrameへ進んだ。117件は直後tokenが既に作成されており、残る12件は未作成だった。正確な破棄主体・キュー位置はなお未確定。標準sinkで同期値のみ変更した診断は、最前面3組で未表示382/4302→0/4302枚と改善したが、非最前面2組では107/8枚→0/5枚（各1434枚）と根治していない。非公開ABI hookは製品に採用せず、詳細は[Present同期診断](Present同期診断.md)に記録した。次は保守可能なsink側変更候補と通常QoS・長時間の実表示を両群に分けて検討する。4K60と実写でend-to-endの0欠落と短い最大間隔を同時に確認するまでは採用しない。
2. 専用DX11 RGB変換のD3D11Memory出力、色・EOS・seek・短時間表示は確認した。4K表示の稀なQoS欠落はなお未解決で、下流待ち・GPU完了・OSスケジューリングを区別する。RGB後queueの交互試行でもOS未表示は残り、安定化策にはならなかった。現行 `d3d11convert` の10bit正規化差は残る。クロマ位置と製品側許容差、内部GPU copy、他GPU、実際のdevice lostも未完了。非対応形式への動的caps変更は明示エラーで停止することを別途検証した。
3. カメラ由来4K60の全480枚画質比較は合格したが、実表示は無欠落ではない。通常QoSの復号出力前欠落、QoS無効でも残るOS表示時刻なし、最前面試行で生じたsink dropを別々に再現・解消する。
