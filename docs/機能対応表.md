# ProRes DX11 / FFmpeg Vulkan 機能対応表（M0）

更新日：2026-09-24。ここでいう「Vulkan成功」は固定SDK `tools/ffmpeg-n8.1-latest-win64-lgpl-shared-8.1/bin/ffmpeg.exe`（実版 `n8.1.3-20260921`）がRTX 3070を選び、`-hwaccel vulkan -hwaccel_output_format vulkan` のフレームを生成した実測を指す。静的35条件は各2フレームをGPUから`hwdownload`してframemd5まで完走した。動的2条件は`-noautoscale -c:v wrapped_avframe`で4/6フレームを通した。動的条件だけは画像readbackを行っていないため、後段で別途画素確認する。再現スクリプトと全件のFourCC、pix_fmt、色タグ、SHA256、成否は`../scripts/probe-prores-feature-matrix.ps1`、`../results/feature-matrix-2026-09-24/summary.json`。詳細ログは同結果ディレクトリに保存する。

根拠ソースはFFmpeg n8.1の[proresdec.c](https://github.com/FFmpeg/FFmpeg/blob/n8.1/libavcodec/proresdec.c)と[vulkan_prores.c](https://github.com/FFmpeg/FFmpeg/blob/n8.1/libavcodec/vulkan_prores.c)。前者はFourCCで10/12bitを決め、picture headerで422/444、alphaなし/8/16bit、progressive/TFF/BFF、色の3フィールドを読み、寸法・pix_fmt・field type変更時に出力形式を再取得する。後者は`sw_pix_fmt`のクロマ/深度、alpha、field順をシェーダへ渡す。したがって「422系FourCCは必ず422」「4444系FourCCは必ず444」は実装上の制約ではない。`prores_ks`で生成できる組合せだけに範囲を狭めない。

表記：`V2`はVulkan実測2/2枚、`V90`/`V120`はM2と同じ素材の90/120枚をVulkanで復号しGPU読戻しまで完了、`V4`/`V6`は動的4/4・6/6枚（画像読戻しなし）。`I10`=`I422_10LE`、`Y10`=`Y444_10LE`、`I12`=`I422_12LE`、`Y12`=`Y444_12LE`。いずれも`video/x-raw(memory:D3D11Memory)`の`format`値。アルファ行の`AYUV64`は標準16bit上位詰めの単一packedテクスチャで、独自capsフィールドは用いない。RGBA変換は`RGBA64_LE`のD3D11Memory。422/10bit/progressive/alphaなしはM1コミット`7d67521`、alphaなしの444/12bit交差はM2コミット`f1e77c0`、alpha付き24交差条件はM3コミット`c59a40f`、インターレースはM4コミット`3b130b1`で採用。AYUV64詰め方の標準化もM4後に採用した。詳細は[422プロファイル拡張](422プロファイル拡張.md)、[444・12bit拡張](444・12bit拡張.md)、[アルファ拡張](アルファ拡張.md)、[インターレース拡張](インターレース拡張.md)。

## FourCC・クロマ・アルファ・出力深度

| 入力条件 | FFmpeg Vulkan | DX11現状 | GStreamer caps / 出力format |
| --- | --- | --- | --- |
| apco / 422 / 10bit / alphaなし | V2 | M1採用。1080p各30枚＋4K60枚、全係数一致・画素最大差1 | I10、progressive |
| apcs / 422 / 10bit / alphaなし | V2 | M1採用。1080p各30枚＋4K60枚、全係数一致・画素最大差1 | I10、progressive |
| apcn / 422 / 10bit / alphaなし | V2 | M1採用。1080p各30枚＋4K60枚、全係数一致・画素最大差1 | I10、progressive |
| apch / 422 / 10bit / alphaなし | V2 | M1回帰通過。1080p各30枚の全係数一致・画素最大差1、公開779枚のEOS確認 | I10、progressive |
| apco / 444 / 10bit / alphaなし | V90 | M2採用。全係数一致、YUV/RGB最大差1、EOS/seek | Y10、progressive |
| apcs / 444 / 10bit / alphaなし | V90 | M2採用。全係数一致、YUV/RGB最大差1、EOS/seek | Y10、progressive |
| apcn / 444 / 10bit / alphaなし | V90 | M2採用。全係数一致、YUV/RGB最大差1、EOS/seek | Y10、progressive |
| apch / 444 / 10bit / alphaなし | V120 | M2採用。全係数一致、YUV/RGB最大差1、EOS/seek | Y10、progressive |
| ap4h / 422 / 12bit / alphaなし | V90 | M2採用。全係数一致、YUV/RGB最大差1、EOS/seek | I12、progressive |
| ap4x / 422 / 12bit / alphaなし | V90 | M2採用。全係数一致、YUV/RGB最大差1、EOS/seek | I12、progressive |
| ap4h / 444 / 12bit / alphaなし | V120 | M2採用。全係数一致、YUV/RGB最大差1、EOS/seek | Y12、progressive |
| ap4x / 444 / 12bit / alphaなし | V90 | M2採用。全係数一致、YUV/RGB最大差1、EOS/seek | Y12、progressive |
| apco / 422 / 10bit / alpha 8/16bit | 各V2、1080p/4K速度素材もGPU通過 | M3採用。全画素・係数一致条件通過 | 標準AYUV64（16bit上位詰め）、progressive |
| apcs / 422 / 10bit / alpha 8/16bit | 各V2、1080p/4K速度素材もGPU通過 | M3採用。同上 | 標準AYUV64（16bit上位詰め）、progressive |
| apcn / 422 / 10bit / alpha 8/16bit | 各V2、1080p/4K速度素材もGPU通過 | M3採用。同上 | 標準AYUV64（16bit上位詰め）、progressive |
| apch / 422 / 10bit / alpha 8/16bit | 各V2、1080p/4K速度素材もGPU通過 | M3採用。同上 | 標準AYUV64（16bit上位詰め）、progressive |
| ap4h / 422 / 12bit / alpha 8/16bit | 各V2、1080p/4K速度素材もGPU通過 | M3採用。同上 | 標準AYUV64（16bit上位詰め）、progressive |
| ap4x / 422 / 12bit / alpha 8/16bit | 各V2、1080p/4K速度素材もGPU通過 | M3採用。同上 | 標準AYUV64（16bit上位詰め）、progressive |
| apco / 444 / 10bit / alpha 8/16bit | 各V2、1080p/4K速度素材もGPU通過 | M3採用。同上 | 標準AYUV64（16bit上位詰め）、progressive |
| apcs / 444 / 10bit / alpha 8/16bit | 各V2、1080p/4K速度素材もGPU通過 | M3採用。同上 | 標準AYUV64（16bit上位詰め）、progressive |
| apcn / 444 / 10bit / alpha 8/16bit | 各V2、1080p/4K速度素材もGPU通過 | M3採用。同上 | 標準AYUV64（16bit上位詰め）、progressive |
| apch / 444 / 10bit / alpha 8/16bit | 各V2、1080p/4K速度素材もGPU通過 | M3採用。同上 | 標準AYUV64（16bit上位詰め）、progressive |
| ap4h / 444 / 12bit / alpha 8/16bit | 各V2、1080p/4K速度素材もGPU通過 | M3採用。同上 | 標準AYUV64（16bit上位詰め）、progressive |
| ap4x / 444 / 12bit / alpha 8/16bit | 各V2、1080p/4K速度素材もGPU通過 | M3採用。同上 | 標準AYUV64（16bit上位詰め）、progressive |

422+alphaは固定SDK製422 YUV packetと444 alpha packetの圧縮面を組み合わせた有効なProRes frameを作り、DX11/CPU全画素とVulkan GPUで比較した。10bitの4 FourCCは同SDK製packetのFourCC再タグによる能力検査で、通常エンコードの品質や実カメラ使用を示さない。24交差条件×各2枚のアルファ差0、YUV最大差1、係数完全一致に加え、全条件の1080p/4K directと固定SDK Vulkan速度素材を確認した。詳細な出所・SHA・数値は[アルファ拡張](アルファ拡張.md)。

## フィールド・色情報・寸法・動的変更

| 入力条件 | FFmpeg Vulkan | DX11現状 | GStreamer caps / 出力format |
| --- | --- | --- | --- |
| apch / 422 / 10bit / TFF | V2、30/60枚もGPU読戻し通過 | M4採用。全画素最大差1、係数完全一致 | I10、`interlace-mode=interleaved`、`field-order=top-field-first` |
| apch / 422 / 10bit / BFF | V2、30/60枚もGPU読戻し通過 | M4採用。同上 | I10、`interlace-mode=interleaved`、`field-order=bottom-field-first` |
| apch / 422 / 10bit / `frame_type=3` | 変異素材V2。BFFとして復号 | M4採用。変異素材2枚の係数完全一致・画素最大差1 | I10、`interlace-mode=interleaved`、`field-order=bottom-field-first` |
| ap4h / 444 / 12bit / alpha16 / TFF | V2、30/60枚もGPU読戻し通過 | M4採用。全画素YUV最大差1・alpha差0、係数完全一致 | A16、`interlace-mode=interleaved`、`field-order=top-field-first` |
| ap4h / 444 / 12bit / alpha16 / BFF | V2、30/60枚もGPU読戻し通過 | M4採用。同上 | A16、`interlace-mode=interleaved`、`field-order=bottom-field-first` |
| apch / 422 / BT.601（matrix/transfer/primaries=smpte170m） | V2 | YUV側のISO→GStreamer mappingあり。RGBは拒否 | I10、colorimetry=BT.601相当 |
| apch / 422 / BT.709（3項目ともbt709） | V2 | YUV mappingと専用RGBの対象 | I10、colorimetry=BT.709相当 |
| apch / 422 / BT.2020（matrix=bt2020nc、transfer=bt709、primaries=bt2020） | V2 | YUV mappingあり。RGBは拒否 | I10、colorimetry=BT.2020相当 |
| apch / 422 / PQ（matrix=bt2020nc、transfer=smpte2084、primaries=bt2020） | V2 | YUV mappingの実測未了。RGBは拒否 | I10、BT.2020/PQのcolorimetryが必要 |
| apch / 422 / HLG（matrix=bt2020nc、transfer=arib-std-b67、primaries=bt2020） | V2 | YUV mappingの実測未了。RGBは拒否 | I10、BT.2020/HLGのcolorimetryが必要 |
| apch / 422 / 318×178（幅・高さとも16の倍数でない） | V2 | 未検証。通過扱いにしない | I10、318×178 |
| ap4h / 444 / 319×179（奇数寸法） | V2 | 未実装・拒否 | Y12、319×179 |
| apch / 422 / 320×180→318×178、計4枚 | V4。前後静的条件は各V2 | HQ寸法変更の別試験は通過。本fixtureは未検証 | I10、各寸法で再交渉 |
| ap4h / 422→444→444+alpha16、計6枚 | V6。各静的条件はV2 | 未実装・拒否 | I12→Y12→A16、形式変更で再交渉 |

M4の`field-order`はcapsとバッファのINTERLACED/TFF旗を実パイプラインで確認した。`proresd3d11rgb`はインターレース入力を明示的に拒否する。上表の`frame_type=3`は既存BFF素材のheader変異であり、カメラ実例ではない。動的試験の初回はFFmpeg CLIの暗黙autoscaleがVulkan画像をCPU scalerへ渡せず停止したため、`-noautoscale`を明示した。`pixfmt:vulkan`が各区間で再構築され全フレームが出たことを成功条件にした。動的解像度は固定寸法への`scale_vulkan`後の画像readbackでも4/4枚成功している。

## GStreamer 1.28.2 / RTX 3070 のD3D11Memory形式検査

`d3d11_memory_probe`を候補形式ごとに動かし、`gst_d3d11_device_get_format`、pool確保、全テクスチャのUAV作成を検査した。再現は`../scripts/probe-d3d11-output-formats.ps1`、結果は`../results/feature-matrix-2026-09-24/d3d11-formats.json`。この検査は確保・UAVだけで、色精度や標準sinkへの接続をまだ証明しない。

| 候補format | 実測 | このgoalでの扱い |
| --- | --- | --- |
| I422_10LE / Y444_10LE / I422_12LE / Y444_12LE | 各3面D3D11Memory、各面UAV作成成功 | alphaなしの出力候補 |
| A422_10LE / A444_10LE / A422_12LE / A444_12LE | `gst_d3d11_device_get_format`が拒否 | 使用しない |
| AYUV64 | 1面R16G16B16A16_UNORM、UAV作成成功 | 標準AYUV64（16bit上位詰め）を採用。標準変換2経路を全画素比較し、sinkのnegotiate/20秒EOSを確認。画面キャプチャは判定対象外 |
| RGBA64_LE | 1面R16G16B16A16_UNORM、UAV作成成功 | M3の`proresd3d11rgb`出力。BT.709 limitedのRGBA変換とalpha保持を検証 |
| Y412_LE / Y416_LE | poolは確保できるがUAV作成失敗 | Compute Shaderの直接出力には使わない |

## 範囲と未達

上表の`V2`/`V90`/`V120`/`V4`/`V6`行をこのgoalの実装対象とする。422+alphaはM3で有効fixtureを作り、VulkanとDX11の両方が通ることを確認した。`alpha_info=3`は固定SDK CPU/Vulkanの0枚拒否とDX11の`STREAM/FORMAT`停止を確認して対象外とした。ProRes header version>1もソース上は不正候補だが、固定SDKとDX11の負例確認は未了。未知FourCCや他のbitstream境界もこの表だけで非対応と断定しない。

M0で判明した交差組合せ（422系FourCCの444/alpha、4444系FourCCの422）は、名目上のM1/M2だけでは漏れる。alphaなしの8条件はM2、alpha付き24条件はM3で採用した。Vulkan出力は過去にCPUとの差があるためDX11の画素基準にはせず、固定SDK CPUとの全画素・全成分比較を別に行う。M0～M4は完了したが、色タグ・端数寸法・途中形式変更（M5）と実表示は未達であり、goal全体を完成扱いにしない。
